# POX-BOY 3026 OS - Agent Handoff Document

**ATTENTION AI AGENT:** 
You are taking over development of a highly customized, offline-first Progressive Web App (PWA) designed for a live-action roleplay (LARP) event called **Pox Eclipse** (A Fallout / Mad Max crossover).

## 1. The Architecture
*   **No Backend:** This app is 100% serverless. Do NOT attempt to add Node.js, Python, or SQL databases. 
*   **Data Persistence:** All user data (items, quests, factions, stats) is permanently saved to the device using `localStorage`.
*   **File Structure:** The app was previously a single 3,000-line HTML file but has been modularized. You must maintain this structure:
    *   `index.html` (The core UI layout, modals, and templates)
    *   `styles.css` (All styling. Note the heavy use of CSS variables for the CRT phosphor glow).
    *   `app.js` (All logic, caching, saving, and hardware API calls).
    *   `sw.js` (The Service Worker. **CRITICAL:** You must increment the `CACHE_NAME` version in this file every time you make an update, or users' phones will not download the new code).
    *   `manifest.json` (Forces immersive fullscreen on mobile).

## 2. Core Mechanics (Already Implemented)
*   **V.T.A.R.S. Registration:** A robust 6-step onboarding loop. Users pick a Name, Origin, and Trait, take a 5-question G.O.A.T. exam, manually allocate S.P.E.C.I.A.L. points, and pick a Perk. The math engine converts this into their starting HP, Skills, Class Title, and Faction Reputations.
*   **Virtual Keyboard:** To prevent native mobile keyboards from breaking the immersion, all `<input>` fields are set to `readonly`. Clicking them triggers a custom HTML/CSS glowing green virtual keyboard (`vk-modal`).
*   **P2P QR Engine:** The app uses `qrcodejs` and `html5-qrcode`. Players can trade items or copy quests by generating JSON payloads in a QR code and scanning each other's screens.
*   **Geofencing Map:** Uses `Leaflet.js`. The user's GPS is tracked. If they walk within 30m of a hidden map coordinate, a location is "Discovered".
*   **Live Radar (Firebase):** An optional Firebase integration allows users who opt-in to broadcast their GPS coordinates to a shared map.

## 3. The Development Backlog
Please review `IDEAS_LOG.md` in this directory. The user intends to implement the following features next:
*   **The Wastelander Rolodex:** A system where users scan each other's "Profile QR" to save them as a Contact.
*   **Bounty Hunting PVP:** Users can accept bounties against specific players. They must physically hunt them down and scan a "Surrender QR" on the target's phone to claim the reputation reward.
*   **Dynamic Traits:** Allowing "Overseers" (Admins with the `1234` PIN) to grant permanent digital badges/traits to players in the field.

## 4. UI Rules
*   **No Native Popups:** Do NOT use `alert()` or `confirm()`. You must route all warnings through `showNotification(msg)` or `showCustomPrompt(text, buttons)`.
*   **Orientation:** DUAL-ORIENTATION since v0.33 (attendee majority uses portrait). Locking to landscape is banned; landscape split-layouts engage automatically via CSS orientation media queries. Build all new UI to survive both.

**Proceed by reading `app.js` and `IDEAS_LOG.md`, then ask the user which feature they want to tackle first.**

## 5. Changelog
*   **v0.22:** Fullscreen rewritten (API-only truth + intent tracking + RESUME state). Fixed missing `cam-placeholder`/`cam-start-btn` IDs (camera crash), duplicate `custom-prompt-modal`, `event.target` tab fragility, Android `new Notification()` crash (now via ServiceWorker). Manifest orientation → landscape.
*   **v0.23:** Fullscreen engine hardened again: FUSED truth (`fullscreenElement` AND browser-chrome height check) kills the permission-popup "wedge" where the API stays non-null after visual exit; `exitFullscreen`/`requestFullscreen` never naked-awaited (raced vs timeout — wedged promises hang); unstick = phantom-exit then re-request inside the same tap; `resize`/`visibilitychange` listeners added. `sw.js` is now NETWORK-FIRST for same-origin files + `controllerchange` auto-reload so devices can never run a stale mixed version again.
*   **v0.24:** `[EXIT FULL]` fixed for installed PWAs: manifest changed `display: fullscreen` -> `standalone` (the old value locked the WebAPK into OS-level immersion the DOM API could not override -- only killing the app escaped it). `exitFullscreen()` now tries all vendor exit variants raced with timeouts. CAM tab restructured to `.cam-split`: 4:3 sensor pane left + all controls right in landscape (mirrors MAP); `[ POWER ON SENSOR ]` no longer wraps mid-label in portrait. NOTE: iOS bakes display-mode at install -- users must delete the home-screen icon and re-add once after this deploy.
*   **v0.25:** Fullscreen AUTOPILOT: installed app = always-fullscreen by design (fsIntent defaults true outside browser tabs); permission-popout recovery now happens on the NEXT real tap anywhere (touchend/pointerdown capture listeners -- popup callbacks carry no user activation, which is why RESUME taps kept getting rejected). FS button hides itself in `display-mode: fullscreen` installs and on iOS home-screen apps (no API). Manifest back to `display: fullscreen` (OS re-immerses after system dialogs automatically). Camera flip rewritten: `enumerateDevices()` + explicit `deviceId` hard-restart (applyConstraints could 'succeed' without switching sensors); chosen camera persists across tab switches. Onboarding name field now survives the GPS opt-in toggle re-render (obNameCache).
*   **v0.26:** Numpad ban completed: `rep-amount`, `fac-rep`, `edit-fac-rep` are now readonly display chips driven by +/- steppers (+-5 per tap via `stepNumberInput()`, min 5 for the auth amount). Pre-boot calibration screen gets `max-height: 94vh; overflow-y: auto` plus landscape compaction rules (@media landscape max-height 600px) so it no longer clips on small phones. Theme tint engine: each entry in `themes[]` now carries `mapFx`/`camFx` filter strings; `cycleTheme()` pushes `--tile-filter` (`.leaflet-tile`) and live-updates `#cam-video`/`#cam-canvas`; `savePhoto()` bakes the current theme tint + watermark colour into saved images. WHITE theme = true grayscale phosphor.
*   **v0.27:** GLOBAL CRT ENCLOSURE: single fixed layer `#crt-global` (z-99999, pointer-events none) over the whole app = curved-edge shadow + vignette + bezel frame + glass sheen + scanlines + rolling refresh bar (theme-tinted via `--pip-rgb`); body lost its `class="crt"` (global layer supersedes it). SENSOR TINT via CSS vars: `:root` now defines `--pip-rgb`/`--cam-filter`/`--tile-filter`; `cycleTheme()` flips all three; `#cam-video`/`#cam-canvas`/Leaflet tiles AND `#qr-reader video` all read `--cam-filter`/`--tile-filter` (QR scanner UI also themed: links/buttons/selects pip-coloured). DATABANK: gallery is now a `.photo-tile-grid` of small 4:3 tiles; `openPhotoViewer(i)` opens `#photo-viewer-modal` (img max 78vh object-fit contain = whole image visible, zero scrolling) with DELETE/CLOSE; `deleteViewerPhoto()` routes through showCustomPrompt.
*   **v0.28:** Global CRT enclosure REMOVED per user request (element + CSS deleted; body regained its original class="crt" scanlines/flicker). Databank tiles fixed: dropped `loading="lazy"` (WebKit lazy-load quirk inside scroll panes = blank tiles) and replaced `aspect-ratio` with fixed 90px tile height (aspect-ratio unsupported on older iOS collapsed tiles to zero height). Theme tint vars (--pip-rgb/--cam-filter/--tile-filter) retained for marker/camera/QR/map tinting.
*   **v0.29:** Radar staleness cap -- Firebase listener skips drawing any other-player beacon whose timestamp is missing or older than 24h (LKL labels for fresh beacons unchanged). G.O.A.T. exam is now the SOLE S.P.E.C.I.A.L. allocator: each answer awards +2 to both listed stats (cap 10; ~27 total points vs old 28), Q5 commits userProfile.special and jumps straight to trait select; manual assignment screen (obStep 5), obPoints and adjustObSpecial() deleted; answer labels now read "(+2 STR, +2 AGI)" style.
*   **v0.30:** Boot-time HARDWARE AUTHORIZATION: new `[3] AUTHORIZE SATELLITE & OPTICS` step on the pre-boot calibration screen runs `primeDevicePermissions()` -- one-shot geolocation fix, a camera getUserMedia whose tracks are immediately stopped (permission primed, hardware released), and Notification.requestPermission (guarded for API-less devices). A live in-universe status block prints OK/DENIED/UNAVAILABLE per system; ends with restoreFullscreenIfDesired() so the Android popup chain can't strand the user out of fullscreen. Rationale: each native prompt can only fire once per origin, so burning all three during boot guarantees zero immersion-breaking popups mid-game. Commence Logon renumbered to [4].
*   **v0.31:** P2P COMMS STACK (Rolodex + Firebase mailbox). IDENTITY: `pipboy-uid` now minted at boot (was GPS-gated); `[MY DATACARD]` buttons (INV/DATA) + WASTELANDERS tab broadcast a plain-text QR `poxboy:{uid}:{name}` (NOT JSON). SCANNER: `onScanSuccess` routes any `poxboy:` plain-text to `handleDatacardScan` BEFORE the legacy JSON parse (TRADE_ITEM / SHARE_QUEST QR flows untouched). ROLODEX: `pipboy-rolodex` localStorage {uid,name,metAt}; DATA sub-tab WASTELANDERS lists contacts (presence read from `lastKnownBeaconData`); contact sheet has [MESSAGE]/[SEND QUEST]/[SEND ITEM]/REMOVE — SEND GATED: you can only transmit to scanned contacts. HANDSHAKE (one-scan mutual link): scanning a datacard locally adds them AND queues `{type:'handshake'}` into their mailbox; recipient gets ACCEPT LINK/IGNORE prompt (from unknowns this is the ONE cold-message allowed — it proves physical card-showing); mutual-scan letters auto-retire. MAILBOX: Firebase node `mail/{recipientUid}/{key}` letters {type:msg|quest|item|handshake, from, fromName, ts, payload}; receiver listens via firebaseOnValue on own uid (same pattern as wastelanders/). Accept/decline writes field-level flags (claimed/declined/fulfilled) onto the letter, then marks key processed (`pipboy-mail-processed`); sender's outbox lazily `firebaseGet`s sent letters on MAIL open / online event / 20s ticker (added `firebaseGet`/`firebaseRemove` to the index.html module imports). Outbox lives in `pipboy-outbox`, flushes on boot/online/ticker; statuses: QUEUED → AWAITING → ACCEPTED ✓ / DECLINED ✗ / CONTRACT FULFILLED ✓ / LINK CLOSED; [CLEAR] removes the Firebase letter + entry. ITEM POLICY = MOVE: quantity escrowed at transmit, auto-REFUNDED if declined (refundItemPayload). QUESTS: composer (title/brief/3 objectives/reward/30m-2h timer), accepted quests land in quests[] as type CONTRACT, giver = sender name, brief+reward injected as objective lines, `contractKey` stored so completing the quest writes fulfilled:true back for the giver. MSGS: stored to `pipboy-maillog` TRANSMISSION LOG (both dirs, cap 100). QUARANTINE: non-handshake mail from unknown uids renders in a collapsed UNVERIFIED section (type + claimed name only, payload never shown) and auto-promotes into the inbox when the sender is later scanned/linked. MAP STICKY-SELECT: tapping a wastelander beacon pins `#map-user-card` (name, LIVE/LKL age, haversine distance if own GPS on) with contact-gated MSG/QUEST/ITEM buttons or "SCAN DATACARD TO CONNECT"; map-tap or [X] deselects; card live-refreshes on each beacon snapshot; marker clicks stopPropagation so map-click-deselect doesn't fire. All peer-supplied strings pass escapeHtml before innerHTML. `pipboy-mail-seen` (capped 500) stops re-notifying on snapshot repeats; processed-flagged letters purge from Firebase after 2h if the sender never cleared them. Overseer map pins deliberately deferred (queued next).
*   **v0.32:** PWA INSTALL FIX + header battery + persisted padding/accessibility. INSTALL ROOT CAUSE (attendee reports "Chrome install did nothing"): (a) manifest declared a single icon at "512x512" while icon.png was actually 1024x1024 -- size mismatch plus the missing required 192px entry suppressed Chrome's automatic install surfaces (mini-infobar / "Install app" menu item); (b) Android WebAPK minting is server-side and silent for up to ~1 minute, so "nothing happened" was actually the APK being generated -- the friend later installing "an APK with another installer" WAS the WebAPK arriving. FIXES: real `icon-192.png` + `icon-512.png` generated from the 1024 master (icon.png kept for apple-touch-icon); manifest now declares 192(any) + 512(any) + 512(maskable); sw.js precaches both new icons. New `[4] INSTALL APP` button on the pre-boot screen (COMMENCE LOGON renumbered [5]): `beforeinstallprompt` is captured and replayed via `installApp()`; button hides itself when display-mode is not 'browser' (already installed) and falls back to iOS Share->Add-to-Home-Screen or manual-browser-menu notifications when no capturable prompt exists; `appinstalled` confirms via showNotification. HEADER: `pip-batt` span under pip-clock, fed by navigator.getBattery() (Android/Chrome) -- permanently hidden where the API is missing (iOS); shows `PWR NN%` (+charge bolt flag). PADDING: `pipboy-size-index` now persists; installed PWAs default to MAX, browser tabs keep SHRINK 2; both size buttons sync labels at boot. APPLE METAS: apple-mobile-web-app-capable + status-bar-style black-translucent (pre-16.4 iOS home-screen mode). DIRECTIVE CHANGE (v0.32 onward): CORE_DIRECTIVES #9 -- deploy replies = detailed point-by-point breakdown THEN one-line-per-item TL;DR.

## 6. Firebase Realtime Database Rules (publish in console; NOT shipped in the zip)
Paste into Firebase Console > Realtime Database > Rules > Publish. Scopes access to the two
nodes the app uses, validates every write's shape/size, denies everything else. Field-level
flag writes (claimed/declined/fulfilled on mail letters) pass these validators.
```json
{
  "rules": {
    "wastelanders": {
      ".read": true,
      "$uid": {
        ".write": true,
        ".validate": "newData.hasChildren(['name', 'lat', 'lng', 'timestamp'])",
        "name":      { ".validate": "newData.isString() && newData.val().length <= 24" },
        "lat":       { ".validate": "newData.isNumber() && newData.val() >= -90 && newData.val() <= 90" },
        "lng":       { ".validate": "newData.isNumber() && newData.val() >= -180 && newData.val() <= 180" },
        "timestamp": { ".validate": "newData.isNumber() && newData.val() > 1735689600000" },
        "hp":        { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 1000" },
        "rads":      { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 1000" },
        "mutations": { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 20" },
        "$other":    { ".validate": false }
      }
    },
    "mail": {
      ".read": true,
      "$uid": {
        "$mailKey": {
          ".write": true,
          ".validate": "newData.hasChildren(['type', 'from', 'fromName', 'ts', 'payload'])",
          "type":      { ".validate": "newData.isString() && (newData.val() == 'msg' || newData.val() == 'quest' || newData.val() == 'item' || newData.val() == 'handshake')" },
          "from":      { ".validate": "newData.isString() && newData.val().length <= 64" },
          "fromName":  { ".validate": "newData.isString() && newData.val().length <= 32" },
          "ts":        { ".validate": "newData.isNumber() && newData.val() > 1735689600000" },
          "payload":   { ".validate": true },
          "claimed":   { ".validate": "newData.isBoolean()" },
          "declined":  { ".validate": "newData.isBoolean()" },
          "fulfilled": { ".validate": "newData.isBoolean()" },
          "$other":    { ".validate": false }
        }
      }
    },
    "sharedpins": {
      ".read": true,
      "$pinId": {
        ".write": true,
        ".validate": "newData.hasChildren(['label', 'lat', 'lng', 'ts'])",
        "label":     { ".validate": "newData.isString() && newData.val().length <= 32" },
        "lat":       { ".validate": "newData.isNumber() && newData.val() >= -90 && newData.val() <= 90" },
        "lng":       { ".validate": "newData.isNumber() && newData.val() >= -180 && newData.val() <= 180" },
        "ts":        { ".validate": "newData.isNumber() && newData.val() > 1735689600000" },
        "from":      { ".validate": "newData.isString() && newData.val().length <= 64" },
        "fromName":  { ".validate": "newData.isString() && newData.val().length <= 32" },
        "icon":      { ".validate": "newData.isString() && newData.val().length <= 24" },
        "$other":    { ".validate": false }
      }
    },
    "pariahs": {
      ".read": true,
      "$uid": {
        ".write": true,
        ".validate": "newData.hasChildren(['name', 'ts'])",
        "name":      { ".validate": "newData.isString() && newData.val().length <= 32" },
        "ts":        { ".validate": "newData.isNumber() && newData.val() > 1735689600000" },
        "$other":    { ".validate": false }
      }
    },
    "radzones": {
      ".read": true,
      "$zoneId": {
        ".write": true,
        ".validate": "newData.hasChildren(['label', 'lat', 'lng', 'ts'])",
        "label":     { ".validate": "newData.isString() && newData.val().length <= 32" },
        "lat":       { ".validate": "newData.isNumber() && newData.val() >= -90 && newData.val() <= 90" },
        "lng":       { ".validate": "newData.isNumber() && newData.val() >= -180 && newData.val() <= 180" },
        "ts":        { ".validate": "newData.isNumber() && newData.val() > 1735689600000" },
        "kind":      { ".validate": "newData.isString() && (newData.val() == 'hot' || newData.val() == 'med' || newData.val() == 'decon')" },
        "radius":    { ".validate": "newData.isNumber() && newData.val() >= 5 && newData.val() <= 200" },
        "$other":    { ".validate": false }
      }
    },
    "radio": {
      ".read": true,
      "$sid": {
        ".write": true,
        ".validate": "newData.hasChildren(['epoch'])",
        "epoch":     { ".validate": "newData.isNumber() && newData.val() > 1735689600000 && newData.val() < 20000000000000" },
        "$other":    { ".validate": false }
      }
    },
    "announcements": {
      ".read": true,
      "latest": {
        ".write": true,
        ".validate": "newData.hasChildren(['text', 'from', 'ts'])",
        "text":      { ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 140" },
        "from":      { ".validate": "newData.isString() && newData.val().length <= 32" },
        "ts":        { ".validate": "newData.isNumber() && newData.val() > 1735689600000" },
        "$other":    { ".validate": false }
      },
      "$other": { ".validate": false }
    },
    "world": {
      ".read": true,
      "starchedUnlocked": {
        ".write": true,
        ".validate": "newData.isBoolean()"
      },
      "$other": { ".validate": false }
    },
    "$other": { ".read": false, ".write": false }
  }
}
```
*   **v0.33:** DUAL-ORIENTATION + settings consolidation + flat mail feed. PORTRAIT UNLOCK (attendee feedback: majority run portrait): manifest `orientation` is now "any" (was "landscape"); the `screen.orientation.lock('landscape')` in enterFullscreen and the unlock on exit are deleted (documentFullscreen still engages; rotation now follows the device). Landscape split-layouts (map 66/33, cam 4:3) are untouched -- they engage via the existing orientation media queries; new portrait rules: map height 44vh (was hard 300px), tighter content gutters, denser photo grid. SETTINGS MOVE: header-controls (fullscreen/size/theme buttons) removed from the header bar entirely (header = title + clock + battery only, better on narrow screens); new DATA > OPTIONS sub-tab hosts options-fs-btn / options-size-btn / options-theme-btn -- cycleTheme()/cycleSize()/updateFsButtons() now write labels to whichever button IDs exist (all got null-guards; cycleTheme's old unguarded theme-display write would have crashed). Pre-boot calibration buttons [1]-[5] unchanged (they self-sync via the same functions). MAIL FLATTENED per user direction: renderMail() is now a two-zone flat feed -- ZONE 1 "ACTION REQUIRED" pins pending incoming letters (tap to accept/decline, sender name always shown) and untrusted quarantine entities; ZONE 2 merges outbox entries + message log into ONE chronological entity list (50 cap), each row its own transmission (up = sent with status + [X] to clear terminal/queued, down = received text). No folders, no per-user grouping. Handoff UI Rules: the Landscape Lock rule is revoked.
*   **v0.34:** UX polish batch + TWO rules-compat bugfixes. PRE-BOOT: screen-padding button removed from the calibration screen (padding lives in DATA>OPTIONS since v0.33); buttons renumbered [1] FULLSCREEN / [2] AUTHORIZE / [3] INSTALL APP / [4] COMMENCE LOGON (pure renumber, ids unchanged; pb-size-btn refs in app.js are dead-but-guarded). ONBOARDING: opt-in checkbox is theme-coloured with glow (was reading grey on the dim highlight; checked state now a themed border+box-shadow instead of a dim fill). SOCIAL CONSOLIDATION: WASTELANDERS sub-tab is gone -- its content (BROADCAST MY DATACARD + contacts list) plus the new LINK REQUESTS panel live on the STATS page (renderStatsTab renders into #stats-general and also calls renderWastelanders+renderLinkRequests; stale render refs removed). LINK REQUESTS = handshake outbox entries pulled OUT of the mail feed (renderMail now filters type!=='handshake'), each with status + [X]. UNTRUSTED OPEN: quarantine rows are now tappable -- openUntrusted() gates with OPEN ANYWAY (routes letter through the normal accept/decline prompts even unlinked; markProcessed consumes from both buckets) / TRUST SENDER / IGNORE. QUEST COMPOSER PARITY with +ADD QUEST: added LOCATION field and free-text EXPIRATION clock-time input ("18:00"/"1800", tomorrow-rollover, validated with an error notify) replacing the 30/60/120 dropdown; payload carries location, acceptQuest renders it. DATACARD VIA MAIL: contact sheet gains [SEND MY DATACARD] -> resends the handshake letter (receiver gets the same link prompt as a physical scan; harmless no-op once mutual since mutual letters auto-retire). BUGFIX 1 (user-reported "LINK REQUEST stays QUEUED even after linking" + WORSE): the v0.34-published Firebase rules require payload in hasChildren, but RTDB deletes empty objects, and handshakes used to send payload:{} -- every handshake write was rejected since the rules went live. Payload is now {kind:'link'} (non-empty). No console/rules change needed. addContact() also auto-prunes our pending handshake outbox entries + their Firebase letters to that uid (pruneHandshakeOutbox). BUGFIX 2: beacon writes now truncate name to 24 chars to satisfy the wastelanders name validator (longer names would have silently broken GPS broadcast under the new rules). DIRECTIVE LOG: deploy commands may be any clear imperative ("deploy"/"deploy all"/"deploy this"); default is deploy-as-presented, but materially better UX ideas must postpone and discuss first.
*   **v0.35:** Night optics + phone-standard keyboard + roster autonomy. NIGHT MODE: new "◧ NIGHT MODE" toggle in the cam controls pane -- digital gain via applyCamFilter() (theme camFx + brightness(2.0) saturate(0.75); cycleTheme() now routes through applyCamFilter() so the boost survives theme swaps) PLUS hardware torch through track.applyConstraints({advanced:[{torch}]}) guarded by getCapabilities (Android Chrome rear sensors only; iOS/front/desktop fall back to boost-only, reported in the toggle notification). Torch re-arms after stream restarts (flip/power) since applyConstraints dies with the track. KEYBOARD RESKIN: #vk-keys rebuilt to phone-standard layout -- number row on top, QWERTY/ASDF/ZXC rows, backspace at end of row 4, bottom row [:][-][,][SPACE x3][.][ENTER x2] and a thin CANCEL ENTRY bar; portrait media query inflates key height/font for thumb accuracy. INV declutter: [MY DATACARD] removed from INV (still top of DATA + on the WASTELANDERS tab). WASTELANDERS RESTORED as its own DATA sub-tab (regression from v0.34 consolidation): hosts BROADCAST MY DATACARD + roster + LINK REQUESTS (moved here from STATS); STATS reverts to general numbers and gains "WASTELANDERS MET: N" (rolodex.length); link-flow rerenders point back at the wastelanders tab. FORGET: every roster row gains a red [FORGET] button (stopPropagation) plus the contact-sheet REMOVE still exists -- both route to forgetWastelander(uid) with showCustomPrompt confirmation. OPTIONS copy updated: fullscreen explicitly documented as any-orientation (locks were removed v0.33; iOS still requires Add-to-Home-Screen).
*   **v0.36:** User-forced ORIENTATION LOCK (DATA > OPTIONS). New `[ORIENTATION: AUTO]` button (options-orient-btn) cycles AUTO / PORTRAIT / LANDSCAPE via cycleOrientation(); the preference persists in `pipboy-orientation` and is re-applied at boot. AUTO calls screen.orientation.unlock() and restores the v0.33 follow-the-device behaviour (split layouts still media-query driven) -- it is also the undo path for a forced lock. Engine details: lock()/unlock() are feature-guarded (iOS Safari exposes no lock() at all, so the preference persists and the label cycles but rotation stays free); lock() promises that reject outside fullscreen (many Android builds require immersion) are swallowed and re-applied on every fullscreenchange/webkitfullscreenchange, so the lock bites the moment fullscreen becomes available. Notifications confirm each cycle ("ORIENTATION: PORTRAIT LOCKED" etc.). Options caption updated to document the cycle and the platform limits. Cache bumped to pipboy-cache-v25.
*   **v0.37:** TWO user-reported bugfixes. COLD-CONTACT RESTORED from the map (user-reported regression): the map sticky-select card's unlinked branch was static text only ("SCAN THEIR DATACARD TO CONNECT") since v0.31, so there was no way to reach a wastelander you hadn't physically scanned. The branch now renders [SEND DATACARD] (new sendDatacardToUid() -> showCustomPrompt confirm -> sendHandshake(uid): recipient gets the exact same link-request prompt as a physical scan) and [MSG], plus an "UNLINKED SIGNAL" hint line (#muc-actions gained flex-wrap). Composer de-gate: composeTo() no longer hard-requires a rolodex contact -- new composeTargetInfo(uid) resolves contact OR live-beacon name and returns a linked flag; MSG is open to any beacon (title suffixed "(UNLINKED)" for strangers), QUEST/ITEM stay contact-gated with a notify explaining why (contracts + item escrow need an established link). transmitMsg() resolves through composeTargetInfo too; mailLog records the beacon name. Security model unchanged: cold messages from non-contacts still land in the recipient's UNVERIFIED quarantine zone and are accept/decline/ignore-gated exactly as v0.31/v0.34 designed. KEYBOARD PORTRAIT BOTTOM ROW (user-reported): v0.35's uniform flex-grow:1 + 3x/2x flex shares made the : - , . keys comically chunky AND pushed the SPACE/ENTER words outside their button borders at portrait's 1.5rem key font on narrow phones. Fix: punctuation keys demoted to new .vk-sm class (flex-grow:0, fixed flex-basis 34px/38px portrait) which also returns ~40% of the row width to the word keys; SPACE/ENTER are now .vk-wide (overflow:hidden, white-space:nowrap, letter-spacing:0) with a portrait font override of 1.05rem so the words scale to fit their boxes instead of spilling. ENTER flex bumped 2 -> 2.2. Letter/number rows untouched. Cache bumped to pipboy-cache-v26.
*   **v0.38:** FOUR-item deploy (location sync + flap fix + mail picker + '?' key). SHARED MAP PINS (user: "sync added locations between users"): saveNewWaypoint() now offers a themed prompt after every marker save -- SHARE WITH EVERYONE writes {label<=32, lat, lng, from, fromName<=32, ts} to new node sharedpins/{pKey} (broadcastWaypoint()); KEEP PRIVATE leaves it local. Every client watches sharedpins/ via firebaseOnValue next to the wastelanders/ listener (map init) and renders each pin as a DASHED hollow diamond (distinct from the solid own-waypoint diamonds) with a bottom-side permanent tooltip "LABEL -- VIA NAME", zIndex 500 (under beacons). Staleness: pins older than 72h are skipped on render (outlive the weekend, die before the next one). Not auto-broadcast on save (opt-in per marker -- scribble-flooding guard). *** FIREBASE RULES MUST BE REPUBLISHED -- sharedpins is a NEW node and the deployed rules deny all non-wastelanders/mail paths; updated JSON at /home/user/firebase-rules.json and in §6 above. *** ORIENTATION FLAP FIX (user: "briefly flips between portrait/landscape"): root cause was the v0.36 engine re-firing lock()/unlock() on EVERY fullscreenchange, and the v0.25 fullscreen autopilot re-enters immersion after every native-popup wedge -- each recovery cycle re-snapped rotation. Engine now tracks orientationAppliedMode, never re-fires a mode already in effect, re-arms only when immersion is lost (OS releases locks on exit), and skips work entirely outside immersion (browser tabs). One snap per actual change. SEND NEW MESSAGE (user): MAIL tab gains [SEND NEW MESSAGE] -> openRecipientPicker() lists every rolodex contact as a prompt button (empty rolodex -> notify to scan a datacard first), tapping one opens composeTo('msg', uid). showCustomPrompt() gained a 50vh scroll-capped button container so big rolodexes can't spill off-screen. VK: '?' key added as a fifth .vk-sm punctuation key; bottom row rebalanced (SPACE flex 3 -> 2.6, punct portrait basis 38px -> 30px) so SPACE/ENTER still fit. Cache bumped to pipboy-cache-v27.
*   **v0.39:** SELF MARKER DECLUTTER (user-reported: "the I'm here on the map gets in the way of close range clicks/other wastelanders - maybe make us just a green dot that brings us up when we click it like others"). Root cause: the own-location marker had zIndexOffset 1000 (ABOVE every other beacon's 900) plus a PERMANENT "YOU ARE HERE" tooltip glued to its top -- the label physically covered any beacon standing near you and won the tap. Fixes: (1) permanent tooltip DELETED -- the marker is now a bare theme-coloured pip dot (14px, border + glow, pulse animation also removed = calmer); (2) zIndexOffset dropped 1000 -> 800 so neighbouring wastelander markers render and tap on top of yours; (3) your dot is now CLICKABLE with the same v0.31 sticky-select behaviour as any beacon (stopPropagation + selectBeacon(myUid)) -- map-tap or [X] deselects as usual; (4) updateMapUserCard() gains a self-branch (uid === myMailUid): the actions row shows "THIS IS YOUR LIVE SIGNAL -- OTHER WASTELANDERS SEE THIS DOT." instead of SEND DATACARD/[MSG] (self-addressed link requests/mail are nonsense); name/dist readout unchanged (shows LIVE SIGNAL | 0M AWAY); (5) disabling GPS now also deselects your own card if it was pinned. No Firebase rules or schema changes. Cache bumped to pipboy-cache-v28.
*   **v0.40:** PORTRAIT-ONLY LOCKDOWN (user direction: "remove all landscape mode and options - portrait only is the way forward"). manifest.json orientation "any" -> "portrait". The v0.36-0.39 ORIENTATION cycle (options-orient-btn + cycleOrientation + orientationModes/Labels, pipboy-orientation pref) is RETIRED -- button removed from DATA>OPTIONS, engine block replaced by a lean applyPortraitLock() that forces screen.orientation.lock('portrait') whenever immersion is available. The v0.38 anti-flap logic survives intact: lock attempts only while immersed (DOM fullscreen or installed display-mode), attempts are one-shot via portraitLockApplied, losing immersion re-arms -- never a rotation re-snap loop on fullscreen autopilot recovery. iOS no-ops via the existing feature guard (manifest portrait hint still applies). Landscape CSS media queries deliberately left DORMANT in styles.css (inert under an OS-level portrait lock on phones; still useful for wide desktop preview windows) -- removal deferred to post-event cleanup per user-agreed nuance. OPTIONS caption updated ("THIS UNIT IS PORTRAIT-ONLY"). DIRECTION LOG: the night-mutes-pings idea (master list item 16) was CANCELLED by the user -- camera night mode is a camera-only feature. Cache bumped to pipboy-cache-v29.
*   **v0.41:** PORTRAIT SHIELD (user field-test on v0.40: "it still rotates on auto rotate, and when the OS-side flip-screen-when-rotated button is pressed"). Root cause analysis: THREE layers control rotation and v0.40 secured only two -- (1) MANIFEST/activity lock can't reach a STALE WebAPK (installed pre-v0.40, Chrome re-mints whenever it gets around to it; user told to uninstall+reinstall the icon), and (2) screen.orientation.lock() is a REQUEST the system may override wherever DOM fullscreen isn't engaged (plain browser tabs) or where the user taps Android's nav-bar rotate-suggestion button. The shield closes layer 3 without negotiation: portraitShieldCheck() reads screen.orientation.angle (legacy window.orientation fallback for old iOS), normalises to 0-359, and toggles html.plock-90 / html.plock-270 classes on <html>; listeners on orientation change + window orientationchange + resize. styles.css gains the PORTRAIT SHIELD block: under the classes, body becomes position:fixed with SWAPPED axes (width:100vh, height:100vw), pinned to a corner, transform-origin:left-top, rotate(-90deg) for angle 90 and rotate(90deg) for 270 so the box exactly refills the rotated viewport; percentage-sized children (modal overlays, content areas) re-fit naturally; large vh-sized surfaces get swapped overrides (#map-container 44vw, .modal-content max-height 90vw, #pre-boot-box 96vw). Desktop never triggers (angle stays 0). The v0.40 lock remains PRIMARY -- when it wins, angle stays 0 and the shield never engages; if fullscreen entry later wins the lock back, angle returns to 0 and the shield disengages cleanly. Residual known limitation: a few inline-vh modal paddings (35vh/90vh inline styles) keep landscape-relative sizing while shielded (still scrollable, cosmetic). Cache bumped to pipboy-cache-v30. DISCUSSED, NOT DEPLOYED: repeated "swipe down to exit fullscreen" hint + fullscreen/almost-fullscreen jumping = fsAutoPilot re-entering DOM fullscreen after every wedge/tap (v0.25 design); candidate fix next round = gentler autopilot (skip DOM fullscreen in display-mode:fullscreen installs, loss-event-driven re-entry instead of every-tap).
*   **v0.42:** FULLSCREEN CALM-DOWN A+B (user-reported: repeated "swipe down to exit fullscreen" hint + jumping between actual and almost fullscreen; portrait shield v0.41 confirmed working first). PART A: OS-immersive installs (display-mode: fullscreen) no longer run DOM fullscreen at all -- the OS is already hiding system bars, so DOM fullscreen on top was pure immersive-hint spam. fsIntent now arms ONLY in display-mode: standalone (browser tabs unchanged: user-driven only) and fsAutoPilot early-returns in fullscreen display-mode. PART B: autopilot re-entry is now LOSS-DRIVEN (fullscreenchange/webkitfullscreenchange + resize for popup-wedge visual exits + visibilitychange return), the every-tap pointerdown/touchend capture listeners are DELETED (they were the toast engine), the cooldown relaxes 1500ms -> 5000ms, and the pilot now routes through enterFullscreen() itself (wedge UNSTICK path included) behind a fused isActuallyFullscreen() check and an in-flight flag. Net effect: GPS/camera popup recovery still happens -- within a breath instead of on the next tap -- and the immersive hint can fire at most once per genuine loss event. Explicit [EXIT FULL]/toggle behaviour untouched. Cache bumped to pipboy-cache-v31.
*   **v0.43:** CAMERA & DATABANK BATCH (master-list items 2-8, incl. field bug "on first open of CAM, SAVE TO DATABANK isn't present" -- answered: it was the old capture->freeze->separate-save flow; now fixed BY DESIGN). CAPTURE-COMMITS FLOW: takePhoto() now archives instantly at capture; the separate SAVE TO DATABANK step is GONE (cam-download-btn + savePhoto() deleted). Post-shot controls are just [TAKE ANOTHER] + [DELETE THIS SHOT] (deleteLastShot drops the just-archived entry). "Save always armed" (item 3/4) = structural, not a button state. RACE GUARD (item 2): capture refuses while the video element has no live frame (post-flip/restart 0x0 canvases were the silent-loss bug) with a "SENSOR RESTARTING -- HOLD" notice. DUAL CAPTURE (item 5): every shot archives {pip, raw} -- RAW = full-res unfiltered + subtle timestamp (JPEG 0.82), PIP = theme camFx (+NIGHT MODE gain) at 0.5x + POX-BOY watermark + timestamp (JPEG 0.6). DATABANK entry shape migrated from bare dataURL strings to {pip, raw} objects via entryPip()/entryRaw() shims -- legacy shots keep rendering. TIMESTAMP (item 6): baked into BOTH versions at capture, subtle (45% alpha, ~w/45 px mono, bottom-right), in-fiction year offset 2026->2287 format DD.MM.2287 HH:MM. GALLERY EXPORT (item 7): viewer gains [EXPORT] (PIP+RAW downloads POXBOY_ts_*.jpg -- Download folder is gallery-indexed), [SHARE] (navigator.share files API = zero download noise, iOS saves straight to Photos, fallback = export), [VIEW ORIGINAL] toggle (hidden for PIP-only entries); OPTIONS gains [AUTO-EXPORT: OFF] cycle (pipboy-auto-export) auto-firing exportEntry on every capture; one-time coachExportOnce() prompt teaches the long-press-to-silence-Chrome-downloads trick (pipboy-export-coached). SELFIE NIGHT LIGHT (item 8): capturing with NIGHT MODE + front sensor floods #cam-screenflash (pale full-viewport overlay) for 180ms pre-capture, shoots mid-flash, hides in finally. STORAGE GUARD: quota eviction now counts purged shots and notifies (DATABANK PRESSURE); total failure = loud "DATABANK FULL -- DELETE OLD SHOTS". Frontend-mirror + watermarking preserved for both pipelines. Zero Firebase/rules impact. Cache bumped to pipboy-cache-v32.
*   **v0.44:** MAIL & NOTIFICATIONS BATCH (master-list items) + two v0.43 amendments ("share option not needed", "export all on the bottom bar under cam rather than export on image selection"). AMENDMENTS: per-image SHARE button + shareViewerPhoto() + dataUrlToFile() and per-image EXPORT button + exportViewerPhoto() DELETED (viewer is now VIEW ORIGINAL / DELETE / CLOSE); new [EXPORT ALL] button on the CAM databank top bar -> exportAllPhotos() confirms count (PIP+RAW pairs), fires downloads sequentially at 350ms intervals with an "allow multiple downloads" heads-up, one-time Chrome-silence coaching retained. BATCH: (1) SEND PHOTO -- contact sheet [SEND PHOTO] gated by isMutualLink(): allowed unless your own handshake to them is verifiably still unanswered (accepted=true; outstanding sent/queued=block with themed explain; offline/legacy links pass); picker modal (#photo-pick-modal) grids databank PIP tiles; sendPhotoMail() recompresses to max-800px JPEG 0.55 and rides the EXISTING 'msg' letter type (payload {text, photo}) -- ZERO Firebase rules changes. (2) Auto-save incoming photos to DATABANK: linked senders auto-save at first sight inside processInboxSnapshot (deduped by pipboy-photosaved mail-key list, cap 500); quarantine-promoted letters save via acceptMsg() hook; acceptMsg also tags mailLog entries hasPhoto/fulfilledTitle and stores safeUid(l.from). (3) MAIL PING: OPTIONS [MAIL PING: ON] (default ON, pipboy-mail-ping) + [TEST PING]; mailPingOs() wraps pushNativeNotification and fires only when app backgrounded OR user not on the MAIL tab (the feed IS the notification when you're staring at it); COMPOSE growing field (item 14): cm-text is now a TEXTAREA with class "grow" + autoGrowEl() in vkPress/vkBackspace/openVk/composeTo (40vh cap); (4) REPLY: [REPLY] buttons on every incoming TRANSMISSIONS row + inside msg/photo prompts; (5) FULFIL NOTICES: executeQuestToggle() on CONTRACT completion now ALSO mails the giver a fulfilledTitle notice (queueMail msg) -- receiver opens it via openFulfilNotice(): if THEY hold an open copy of the same contract (title match) it offers [MARK MY COPY COMPLETE] then logs; (6) CONTRACTS TAB: new DATA sub-tab after QUESTS listing every issued quest from the outbox with live status + [X] clear (renderContracts, switchSubTab hooked); (7) TRANMISSIONS rows tagged ⚑ fulfil / 📷 photo; (8) TEXT SIZE CYCLE: [TEXT: NORMAL] -> 16/18/20px root font (100/112.5/125%), pipboy-font-index, boot-applied, invisible until tapped (item 18). PHOTO DELETE-vs-DATABANK honesty note: DELETE on a photo letter clears the LETTER; the already-auto-saved databank copy stays by design. Cache bumped to pipboy-cache-v33. STILL PENDING CONSOLE: sharedpins rules republish (+pariahs rides with the v0.45 Firebase batch).
*   **v0.45:** MAIL/VK POLISH BATCH (user field-report: "Expanding text field didn't work" + requests: view sent photos/messages, per-category notification options). ROOT-CAUSE FIX -- GROWING FIELD: while typing, the user sees the KEYBOARD overlay's own display deck (#vk-input, a single-line <input>), never the composer -- v0.44 grew the composer textarea BEHIND the overlay (invisible). The keyboard now swaps decks: new #vk-multi <textarea class="pip-input grow"> shown in place of vk-input whenever the target carries .grow (isMulti test in openVk; vkDisplayEl() resolves the live deck for vkPress/vkBackspace/vkConfirm). Password masking stays on the single-line deck (auth-code path untouched). TELEGRAM SPEC per user: text auto-wraps, NO manual line-break key, ENTER stays DONE (vkConfirm commits from the live deck). Growth is seated AFTER the modal goes display:flex -- scrollHeight reads 0 while hidden. MESSAGE VIEWER: TRANSMISSIONS mailLog rows are now one-line PREVIEWS (60-char ellipsis, nowrap+min-width:0 so flex can't spill) -- full text used to blow the row into a wall; tap any row -> viewMailLogEntry(i): FROM/TO stamp + full text (newlines honored via #cp-text white-space:pre-line), photo thumb above text via new #cp-img slot in #custom-prompt-modal (reset on every showCustomPrompt so stale photos never bleed into unrelated queries), [REPLY] on incoming, fulfilledTitle entries can [MARK MY COPY COMPLETE] by title match, purged images print an honest "(IMAGE PURGED FROM LOG -- DATABANK PRESSURE...)" note; incoming rows keep one-tap [REPLY] via event.stopPropagation. #cp-text cap 40vh + overflow-y:auto (very long messages can no longer push buttons off-screen). SENT/RECEIVED PHOTO VIEWING: mailLog entries carry a ~480px JPEG 0.45 thumb (makeMailThumb) -- SENT photos were previously DISCARDED at send time (log kept text only); sendPhotoMail thumbs the transit-compressed url, acceptMsg thumbs inbound payload.photo. pruneMailPhotos() storage guard: image data rides the newest 20 photo entries only, older rows degrade to text + 📷 flag (received full copies still auto-save to the CAM databank). NOTIFICATION PREFERENCES: OPTIONS gains [NOTIFY MESSAGES]/[NOTIFY CONTRACTS]/[NOTIFY LINKS] (cycleNotify(cat, btnId, label); pipboy-notify-<cat> keys; default ON; boot label sync). Each gates BOTH the in-app toast AND the mailPingOs OS ping for its category: MESSAGES covers incoming msg/photo + quarantine holds; CONTRACTS covers the decline-refund notice + a NEW sender-side acceptance alert (senders previously got SILENCE when a contract/shipment was picked up -- refreshOutboxStatuses claimed-transition now fires "CONTRACT/SHIPMENT ACCEPTED BY name" toast+ping); LINKS off PARKS datacard scans -- no pop-up, the letter key lands in the new persisted linkScans map (pipboy-linkscans, written inside saveComms) rendered as tappable "⇄ LINK REQUEST (PARKED)" rows in MAIL ACTION REQUIRED via openLinkScan(key) (same ACCEPT LINK/IGNORE decision on the user's schedule); parked scans self-clean if the link forms elsewhere (isContact early-retire branch deletes the parked key). renderMailBadge counts parked scans (MAIL (2 LINKS)); ACTION REQUIRED header / TRANSMISSIONS header / empty-state conditions extended for lsKeys. System notices (RAD warnings, waypoint discoveries, broadcast results) deliberately stay always-on. OPTIONS caption extended. Zero Firebase/rules/schema impact. Cache bumped to pipboy-cache-v34. STILL PENDING CONSOLE: sharedpins rules republish (+pariahs now rides with the v0.46 Firebase batch).
*   **v0.46:** FIREBASE BATCH (PARIAH + passive RAD decay + marker self-echo fix). MARKER SELF-ECHO (user field-report: broadcasting "joes" drew TWO pins, "joes" + "joes — VIA VEN"): renderSharedPins() now skips pins whose from === myMailUid -- your LOCAL waypoint already is your view of your broadcast; the dashed diamond is for everyone else. Not a double-write: the dup was local marker + own shared echo. RADIATION ENGINE: userProfile.rads (0-1000, eats HP since forever) finally moves -- two opposing 60s processes, never both: inside a pariah field +1 RAD/min ("taking rads damage"), everywhere else -1 RAD/min passive recovery to floor 0 (user: "rads drop by 1 every minute passively unless taking rads damage"). adjustRads() clamps/persists/rerenders. PARIAH SYSTEM: new Firebase node pariahs/{uid} {name<=32, ts} (rules block added to firebase-rules.json + this doc -- REQUIRES ONE CONSOLE REPUBLISH, same 2-minute drill as sharedpins). startPariahListener() boots inside initComms beside the mailbox listener; evalPariahField() runs per GPS fix + minutely via radTick() -- distance via the existing getDistance() haversine over lastKnownBeaconData; HYSTERESIS grabs at 15m / releases at 18m (no boundary flicker); entry warning "⚠ RADIATION FIELD — PARIAH NEARBY" + "RADIATION FIELD CLEAR." on release; declared pariahs are SELF-IMMUNE; beacons stale beyond 5min do not irradiate. OVERSEER CONTROL (STATS tab, pipboy-dev-mode only): #overseer-pariahs panel renders DECLARED PARIAHS w/ [CLEANSE] + LIVE SIGNALS (fresh 5min beacon roster) w/ [MARK PARIAH] -- both behind showCustomPrompt confirmations; writes via firebaseSet/firebaseRemove with NO SIGNAL guard; panel is hidden + emptied when Overseer mode toggles off (toggleDevMode disable branch). Everything rules-compatible offline: listener errors keep last known decree list. Cache bumped to pipboy-cache-v35. CONSOLE ACTION REQUIRED: republish firebase-rules.json (now contains sharedpins AND pariahs) -- without it PARIAH writes reject and decrees stay local.
*   **v0.47:** PARIAH RELAX + MESSAGE ATTACHMENTS + OVERSEER HOT ZONES (user: "Deploy this / also the ability to send message WITH picture/item / also overseer ability to place radioactive map markers"). PARIAH RELAX (user asked why nobody was markable): LIVE SIGNALS picker stays (fresh ≤5min beacons) but KNOWN WASTELANDERS (ROLODEX) list added beneath -- decree pre-declares anyone linked, live or cold (cold marks are harmless paperwork: fields only irradiate off fresh signals); markPariah name falls back to rolodex; live-list empty state now self-documents "OPEN THE MAP ONCE THIS SESSION TO START THE RADAR FEED" (the beacon listener boots on first map init -- the actual gotcha behind the empty roster). MESSAGE ATTACHMENTS: compose-msg-modal gains [+ PHOTO]/[+ ITEM] chips (tap again to clear, refreshAttachUi note line explains); cmAttach state resets per composeTo. PHOTO: databank picker repurposed via photoPickMode 'attach' (hand-back to composer, no immediate transmit; CANCEL returns to draft via closePhotoPick; mutual-link gate reused); transit compression extracted to compressMailPhoto() shared with sendPhotoMail. ITEM: x1 from LOADOUT via list prompt; escrowed at transmit (quantity decrement/splice, MOVE policy); refreshOutboxStatuses decline path extended -- type 'msg' letters carrying payload.item auto-refund to the sender like item letters; acceptMsg grants the pod straight into LOADOUT. Receiver openMailItem msg branch merged into one combined flow: text/photo/item in any mix, attached photos render INSIDE the prompt pre-log (cp-img slot), [LOG + TAKE ITEM] when a pod rides. typeSummary/tags: ACTION rows say "MESSAGE: 📷 / 🎒 name"; TRANSMISSIONS rows stack tags (⚑ 📷 🎒); viewMailLogEntry prints the attached-item note. Payload still rides the free-shape 'msg' letter -- ZERO rules changes for mail. HOT ZONES: new Firebase node radzones/{key} {label<=32, lat, lng, ts} (rules block added to firebase-rules.json + this doc -- REPUBLISH covers pariahs+radzones in one paste). STATS PARIAH WATCH panel gains HOT ZONES (STATIC) section: [☢ DROP HOT ZONE AT MY POSITION] (needs a GPS fix, confirm-gated) + per-zone [EXTINGUISH]; NO auto-expiry -- zones burn until extinguished (Overseer owns the board). renderRadZones() draws red dashed ☢ diamonds (zOffset 450, below shared pins 500); startRadZoneListener() boots in initComms. evalPariahField() now merges both sources (nearest wins): person marks skip self-declared pariahs (self-immune), static zones irradiate EVERYONE including pariahs; entry warning names the source ("⚠ RADIATION FIELD — PARIAH/HOT ZONE NEARBY"); hysteresis 15m grab / 18m release, +1 RAD/min while bathed, -1/min passive recovery otherwise (v0.46 engine unchanged). Cache bumped to pipboy-cache-v36. CONSOLE ACTION REQUIRED: republish firebase-rules.json (radzones node).
*   **v0.48:** STABILITY + NOTIFICATION-DECOUPLING BATCH (user field reports). (1) TIMEOF ReferenceError FIXED: timeOf was a closure inside renderMail(); renderContracts() and (v0.46+) renderPariahPanel()/hot-zone section called it — the moment a real contract row existed the CONTRACTS tab threw mid-render and showed BLANK (user: "contract didn't show up when issued"); same flaw would have blanked the rad panels on first mark/zone. timeOf is now one global helper. (2) PUSH DECOUPLING (user: "overseer and UI etc pushes constantly, massive pain"): showNotification() used to call pushNativeNotification() — EVERY UI toast was an OS push + vibration. showNotification is now strictly in-app (modal + gated haptics); OS pushes flow ONLY via mailPingOs (MAIL PING master + NOTIFY MESSAGES/CONTRACTS/LINKS category gates). New OPTIONS [VIBRATE: ON] master (pipboy-vibrate) since the buzz was part of the complaint. (3) BADGE REPAINT: renderMailBadge() now fires inside markProcessed — the MAIL (n) counter drops the instant a letter resolves (was waiting, visibly, on the next Firebase snapshot; user: "mail says (1) even when opened for receiving photos"; note: opening never resolved by design — LOG/DELETE does). (4) RAD CADENCE + GEIGER (user: "1 every 5 seconds", "don't need notifications for rad field clear etc", "just have geiger counter noise"): engine split into radDamageTick (5s: +1 RAD + geigerBurst) and radDecayTick (60s: -1 if clear); the two NEVER co-run (damage suppresses recovery). Entry/exit field toasts deleted — geigerClick() synthesizes ~18ms highpassed noise bursts via WebAudio (zero shipped assets), 1-3 random clicks per damage tick; context lazily created + pointerdown-resume for autoplay policy; fully defensive (audio fail = silence). (5) PHOTO MAIL STAYS IN MAIL (user: "open received image in mail — don't auto save to databank"): autoSaveMailPhoto() DELETED (first-sight databank grab + acceptMsg hook removed); photos render in the open-letter prompt + keep feed thumbs (v0.45); prompt/purge copy updated (no more databank references); stale pipboy-photosaved key harmlessly abandoned. (6) OVERSEER LOCKOUT HARDENING (user: "lost overseer pariah stuff, not sure how" — one-tap PINless TOGGLE OVERSEER on the MAP strip getting fat-fingered): disabling now routes through the auth modal (pendingAuthAction 'TOGGLE_DEV_OFF' → doDevDisable(), which also null-guards the element-hides so a missing id can't kill the chain). NOTE: the panel also could not have existed on their live build — site was behind (never past v0.45); this deploy catches it up. (7) CONTRACT-ISSUE FEEDBACK: transmitQuest's silent closeModals() bail on lost target now notifies ("TARGET LINK LOST..."), and every successful issue ends with "CONTRACT ISSUED — TRACK IT UNDER DATA > CONTRACTS." Cache bumped to pipboy-cache-v37. CONSOLE ACTION: radzones+pariahs rules republish still owed from v0.46/0.47 (single paste of firebase-rules.json covers all four custom nodes).
*   **v0.49:** REAL GEIGER VOICE (user shipped a 24s catbox rattle loop: "geiger sound - deploy"). geiger.mp3 (382KB) added to pwa/ and the SW precache list (offline-first retained; same-origin network-first still refreshes it in the background). WebAudio synth engine replaced by an HTMLAudio slice player: geigerClick() round-robins a 2-element geigerPool, seeks a random offset and plays a 0.45-0.80s crackle slice per dose; geigerBurst() adds a 25%-chance overlapping second slice (real counter pile-up texture). Autoplay rejections and missing audio fizzle to silence by contract. Rad cadence/decay/hysteresis untouched. Zip ship-list grows to 11 files. Cache bumped to pipboy-cache-v38. CONSOLE ACTION unchanged since v0.46: firebase-rules.json (pariahs + radzones) still owed if not yet republished.
*   **v0.50:** ZONES-ANYWHERE + VAULT-BOY PHOTOS + STATUS OVERLAYS (user: "overseer place rad or healing zones anywhere by waypoint... larger circle... scale as you zoom and match real life size 15m radius" + "vault boy graphic here -> set image from databank" + overlay spec; 400-image avatar run CANCELLED for size bloat, overlays replace it). ZONES: radzones/{key} gains kind ('hot'|'med') + radius (5-200, shipped fixed 15) — RULES REPUBLISH REQUIRED (blocks added to firebase-rules.json + §6). dropZone(kind, lat, lng) generic writer; dropHotZone/dropMedZone wrappers accept 'map' (long-press placement, Overseer-only buttons inside the ADD MARKER dialog #wp-overseer-zones, gated in openAddWaypointModal) or 'me' (panel at-my-position buttons, both confirm-gated). renderRadZones draws a real L.circle fence per zone (dash ring + 7% fill, scales with zoom = ground truth) + center diamond glyph (red ☢ / green ✚) + permanent tooltip. ENGINE: medShelterActive state with the same 15/18m no-flicker fences; zones kind 'med' are excluded from damage candidates; radDecayTick sheds 5/min inside a MED fence, 1/min elsewhere, 0 while bathed. EXTINGUISH unchanged; panel section renamed ZONES (STATIC, 15M), kind-aware rows. VAULT-BOY: .vault-boy-container now tappable -> openAvatarPicker(): [SET IMAGE FROM DATABANK] reuses the photo picker (photoPickMode 'avatar'; CANCEL closes clean via closePhotoPick) -> setAvatarFromEntry center-square-crops + downscales to 96px JPEG 0.7 -> pipboy-avatarimg; [RESET TO DEFAULT] restores the placeholder. renderVaultBoy() rides renderProfile(). OVERLAYS: #vb-fx layer + styles: ☢ drifting orange trefoils when radFieldActive, ✚ soft green crosses when medShelterActive, .fx-rads messy static border + steps() jitter at >=250 rads (class toggled in renderVaultBoyFx, called from evalPariahField/adjustRads/renderVaultBoy). Priority: field > med. Cache bumped to pipboy-cache-v39. NOTE: ambient world-clock / CLEAN-vs-waste model / locations POI node remain ROADMAP (DESIGN doc §1-2) — this deploy ships zones+identity only, per user scope.
*   **v0.51:** ZONE UX PASS + LINK TELEMETRY (deploy split per user command "deploy 1,5" from the master list). ZONES (two user asks): (1) "labels not live -- only if selected, keep the zones up" -- the radzones render keeps both fence + diamond drawn always, but the bindTooltip is now permanent:false and only openTooltip()s while its zone is SELECTED; both the full L.circle fence (easy phone target) and the center diamond are tap-wired via selectZone(zk) with L.DomEvent.stopPropagation so they survive the empty-map-tap deselect; selection re-opens its tooltip after every radzones/ onValue re-render, and auto-deselects if the zone was extinguished under it. (2) "remove them from the map menu as overseer" -- the sticky bottom card (#map-user-card) gains a zone branch (updateZoneCard): muc-name = glyph+label coloured by kind (#ff3333/#5fc98e), muc-info = kind line (HOT: '+1 RAD/5SEC INSIDE' | MED: 'SHEDS 5 RADS/MIN INSIDE') + radius + live distance, muc-actions = red/green [EXTINGUISH] when pipboy-dev-mode==='true' (routes to the existing extinguishZone confirm flow; non-overseers see an 'OVERSEER ZONE -- FIELD ACTIVE' line). SELECTION PLUMBING: one-selection-at-a-time -- deselectBeacon() now also clears zone selections (so the card [X], empty-map taps and the GPS-off self-deselect all behave; added selectedZoneKey to the map click guard), selectBeacon/selectZone clear each other, and muc-name's colour is reset on every beacon card render + deselect so a coloured zone label can never bleed onto a wastelander card. EXTINGUISH COPY FIX: extinguishZone() was hardcoded 'HOT ZONE' even for med fences -- now derives the noun from lastKnownRadZones[key].kind and deselects a pinned card on success. TELEMETRY: beacon writes (wastelanders/{uid}) now carry OPTIONAL hp + rads numerics (0..1000, same floor(currentHp = maxHp - floor(rads/1000*maxHp)) formula as the HP bar) -- hasChildren unchanged so pre-v0.51 units still pass rules; vitals render for datacard-LINKED contacts only (isContact gate): WASTELANDERS roster presence line appends 'HP X | RADS Y' (LKL rows suffix '(AT LAST SEEN)') and the map beacon card info line appends '| HP X | Y RADS'; strangers and dead signals show nothing. *** FIREBASE RULES MUST BE REPUBLISHED: wastelanders gains hp+rads validators and its $other:false would REJECT every GPS write from v0.51 units until published (older units are unaffected either way -- publish rules BEFORE or alongside pushing the files). *** master rules JSON at /home/user/firebase-rules.json. Cache bumped to pipboy-cache-v40.
*   **v0.52:** GPS ENGINE REBUILD + SHARED VITALS BARS + BEACON SCRAMBLER (5-item user batch; NO Firebase rules changes). GPS ROOT CAUSE (user: "my map ping/my live location disappears quite often and quickly"): the watchPosition error handler called toggleGPS(), so ONE transient TIMEOUT (old options were maximumAge:10000/timeout:5000, twitchy under high-accuracy) executed the full OFF path -- clearWatch, userMarker removal, and wastelanders/{uid} wiped to null (dot vanished from every map) while the rad engine kept running on myLastLat (user-confirmed: "effect stays, dot disappears" and screen-off geiger ticks kept firing, which they love). REBUILD: toggleGPS is now a thin dispatcher around startGpsWatch()/stopGpsWatch(reason); gpsOnError classifies -- only PERMISSION_DENIED (code 1) is fatal (stopGpsWatch("denied") + notify), POSITION_UNAVAILABLE/TIMEOUT are ignored (watch/dot/beacon/last-fix all persist); watch options softened to {enableHighAccuracy, maximumAge:5000, timeout:20000}; myLastLat/myLastLng are NEVER nulled on any stop path (rads/healing continue on last fix by design). ON-UNTIL-TURNED-OFF: pipboy-gps-tracking pref written on manual enable, cleared only by manual tap or permission revoke; maybeAutoGps() runs at script bottom (after initComms) and silently re-arms the watch (no fullscreen grab, gpsRestoredPending gates a one-shot "SATELLITE LINK RESTORED" toast on first fix). KEEPALIVE/HEALTH interval every 15s: re-stamps the beacon (fresh timestamp, hp/rads ride) every 30s so dots can't flicker LKL standing still, and after 90s fix-less the map button reads "[GPS UNSTABLE -- HOLDING LAST FIX]" (auto-clears on next fix) instead of killing the session. ensureUserMarker(lat,lng) extracted from the old success callback so the self-dot can be drawn late -- initPipMap now restores it if the watch auto-armed before the map ever initialised (previously the first fix pre-map-open would've thrown on markersGroup null; gpsOnFix guards marker ops behind if(markersGroup)). VITALS BARS: the footer HP/RADS bar (bottom-left HUD, "#hp-fill-bar"/"#rads-fill-bar") was NEVER WIRED -- only its text updated; renderProfile now writes both fills so red overtakes green there exactly like the STATUS page. New shared vitalsBarHtml(hp, rads) renders the stacked "overtaking" bar (green = HP slice, red = rads/1000 slice from the right; caption "HP x | y RADS") and rides: map user card (new #muc-vitals div; contacts with numeric telemetry only, updateZoneCard explicitly clears it so zones never inherit a stale bar), contact sheet (#contact-vitals, telemetry-gated), and MY DATACARD (#dc-vitals, live from userProfile -- never stale). Self map-card line now flags "SCRAMBLED: EVERYONE SEES YOUR DECOY-SITE DOT." when the scrambler is armed. BEACON SCRAMBLER (user: privacy option pre-event, decoy at event site): DATA > OPTIONS gains [BEACON SCRAMBLER: OFF] (toggleScrambler, amber, boot-synced via IIFE like auto-export); when armed, pushMyBeacon() substitutes decoyCoords() for the real fix -- local myLastLat stays REAL so rads/zones/healing/distances/waypoint-discovery all stay truthful, only the broadcast is decoyed. Decoy base = pipboy-decoy JSON or DEFAULT_DECOY {-31.56346462162551, 117.7976226150244} (user-supplied event site); long-press map > [SET DECOY SITE HERE] (setDecoySite, uses the waypoint modal's tempWp locks; all-player, not dev-gated). decoyCoords adds a UID-SEEDED stable jitter (5-20m ring, angle from a 31-hash) so N scrambled testers scatter instead of stacking on one pixel, every client renders the identical layout, and dots never wander between refreshes (user asked exactly this: "will there be a way to see stacked users if 10 people use decoy"). Toggling or re-siting re-stamps the beacon immediately. Cache bumped to pipboy-cache-v41.
*   **v0.53:** ZONE REPAINT FIX + INV RETIREMENT (MAIL promoted) + RADIO v1 (NO Firebase rules changes). ZONE BUG (user: "hot/med zones disappeared from the map but still show up in the list"): renderRadZones was only EVER called from the firebase radzones/ onValue listener, which fires during comms boot BEFORE initPipMap -- the `if (!radZonesGroup) return` guard swallowed that first paint and map-init never re-rendered from lastKnownRadZones, so zones only appeared after a live drop/extinguish event (which is why earlier tests seemed fine). FIX: initPipMap now calls renderRadZones(lastKnownRadZones) right after the v0.52 ensureUserMarker restore, so opening the map always paints whatever the satellite last told us. INV RETIRED (user: "get rid of INV and all item functions; add Mail tab; stats under data moves under stats"): the INV main tab is GONE -- MAIL is promoted to the top-level nav (#mail-navitem + new #tab-mail pane hosting the SCAN TRANSFER guard, openRecipientPicker and #mail-container; renderMailBadge + mailTabActive retargeted accordingly). DATA loses its MAIL sub-nav item and old stats sub-pane; the stats content (#stats-general + #overseer-pariahs) moved to the STAT tab as #sub-stat-stats (new currentStatTab='status' default + statsPaneActive() helper; switchSubTab gained a 'stat' branch that re-renders renderStatsTab; dev-controls row now gated on tabId==='stat' && currentStatTab==='stats'; the three old currentDataTab==='stats' gates switched to statsPaneActive()). switchMainTab reworked: add-item-btn writes null-guarded, inv render case removed, mail+stat cases added, photo/inventory render calls outside inv are untouched; renderInventory early-returns cleanly when #inv-container is absent. ITEM CREATION UI PURGED: map-card [ITEM], contact-sheet [SEND ITEM] and composer [+ ITEM] chip all removed (photo chip stays). CASUALTIES: type:'item' letters can no longer be CREATED from any UI, which KILLS the unshipped RADAWAY CHEMS shelf item (struck from roadmap); the legacy accept/decline/escrow RECEIVER code is left fully intact as a safety net so any item letters still in flight from pre-v0.53 units resolve normally, and the old add-item modal/item CRUD stays in the file inert (harmless dead weight, zero UI paths reach it). RADIO v1 (user: "deploying radio... pox-radio.netlify.app is radio url"): the RADIO tab is now LIVE -- three dials driven by the new radio-stations.json (12th zip file, added to SW precache; schema {generated, stations:[{id,name,item,base,tracks:[{t,a,f,b}]}]} with CDN_BASE const for one-line future host swaps; `item` keeps the archive.org mirror ids): GNR "CAPITAL WASTELAND RADIO" 26 tracks / 33.5MB (songs -> Enclave marches -> Agatha classicals), DCR "COMMONWEALTH RADIO" 36 / 43.4MB, APPA "APPALACHIA RADIO" 28 / 34.3MB = 90 tracks, 111.2MB, all mono Opus 64kbps through the user-locked AM master chain (250Hz-3.8kHz bandpass + compand + brown-noise bed); CDN https://pox-radio.netlify.app probed live on all three dials (200 + access-control-allow-origin:* + immutable max-age). ENGINE (initRadio() after initComms): one shared Audio element; trackUrl = st.base + '/' + encodeURIComponent(tr.f) (direct CDN URL -- offline playback rides the SW's cross-origin cache-first branch against the dedicated bucket); tap a station row to play (420ms synthesized bandpassed brown-noise static burst via lazy AudioContext radioStatic(ms)), tap the live row to stop; per-station remembered track index (pipboy-radio-pos) so every dial resumes its own loop; onended chains 550ms static + radioNext; [>> NEXT TRANSMISSION] = radioNextUi, [RADIO OFF] = radioStop; amber #radio-now strip names the live transmission (STANDBY at idle). SIGNAL MODEL: stream-lazy -- pressing play on a dial you never fetched still plays while you have network; if a track isn't onboard AND there's no network the onerror path prints "NO SIGNAL -- FETCH THE PACK ON WIFI" on the row and auto-skips to the next track. DOWNLOADER: [FETCH RADIO PACKS] > radioDownloadMenu() (showCustomPrompt) quotes TRUE manifest byte counts -- [FETCH EVERYTHING ~X MB] plus one button per not-yet-onboard dial (ONBOARD dials hidden); radioDownload(sids) walks tracks sequentially ("DOWNLOADING ... i/N", live byte counter), cache.match-skips anything already onboard (fully resume-safe across aborts), the button flips to a working [STOP FETCH]; on completion dials flip green "ONBOARD -- plays with zero network" ONLY if zero failures (failures = amber + retry hint in #radio-dl-status), state persisted in pipboy-radio-dl. SW: RADIO_CACHE='pox-radio-v1' separate bucket; activate whitelist keeps RADIO_CACHE so app updates NEVER wipe stations; fetch handler gains an isRadio check so pox-radio.netlify.app traffic is NOT auto-stored into the APP cache (prevents double-storing 111MB; onboard packs still served offline via caches.match; uncached streams pass straight through). Cache bumped to pipboy-cache-v42. NOTE: the old v0.30s-era playRadio() skeleton stub may still exist in app.js -- dead code, no markup calls it. OPEN FLAG: one GNR filename (Ella Fitzgerald "Into You.opus") was title-mapped as "INTO EACH LIFE SOME RAIN MUST FALL" -- user to verify on first listen. Radio v2 epoch-sync ([STATION SYNC] shared queue position) remains parked per user ("just looping").
*   **v0.54:** RADIO STATION SYNC -- live shared broadcast via EPOCH PLAYHEADS (user: "how do we deploy live radio so everyone listens to same at same time?" -> "deploy radio sync"). *** FIREBASE RULES MUST BE REPUBLISHED: new `radio` node added to firebase-rules.json AND the §6 mirror -- {".read": true, "$sid": {".write": true, hasChildren(['epoch']), epoch number 1735689600000..2e13, $other:false}}; same self-police trust model as radzones/pariahs (writes are UI-gated by the dev-mode PIN, no auth). *** MECHANISM: nobody broadcasts audio -- every unit syncs PLAYHEADS. The desk writes radio/{sid} {epoch:ms}; each client computes elapsed = serverNow - epoch against the SATELLITE clock (a /.info/serverTimeOffset listener kills phone-clock skew) and walks the manifest durations to land on the same track at the same second (±2s worst case, inaudible under the baked-in AM static bed). MANIFEST: radio-stations.json gains a per-track `d` seconds field -- all 90 durations harvested live from the CDN via ogg-tail 64KB range probes (last page granulepos / 48000Hz; zero errors; loop lengths GNR 68.3min / DCR 95.2min / APPA 74.5min). Re-run /home/user/radio-test/add_durations.py if the library ever changes; NO audio re-upload needed. initRadio computes st.totalDur at manifest load. PLAYER: new [STATION SYNC: ON/OFF] toggle on the RADIO tab (pipboy-radio-sync, DEFAULT ON); rows gain an amber ". LIVE ." badge whenever their dial is on air; tapping a LIVE dial crackle-joins mid-track at the shared offset (seek re-derived at loadedmetadata time so buffering lag doesn't skew the join); synced track-ends chain with NO inter-track static (tight follow -- radioSyncBoundary waits out clock skew 4x800ms then falls back to local advance so loops never stall); a 5s radioSyncWatchdog re-joins on any track mismatch and hard-punches currentTime only past 1.5s drift (no constant micro-stutter); [NEXT TRANSMISSION] refuses under sync ("SYNC LOCKED -- THE OVERSEER RUNS THIS DIAL. (SYNC OFF FOR LOCAL CONTROL)"); SYNC OFF preserves the exact v0.53 free-run behaviour everywhere. LISTENER: radio/ onValue drives radioLive; if the playing dial's epoch changes (SKIP / fresh GO LIVE) the unit crackle-re-joins ("OVERSEER SHIFTED THE BROADCAST."), radioAppliedEpoch epoch-identity compare makes same-epoch re-emissions no-ops; if the Overseer CUTs the dial, synced units stop dead ("BROADCAST ENDED -- THE DIAL WENT DARK."); units free-running a dial that goes live do NOT auto-join -- retap to tune in. OVERSEER BROADCAST DESK: new #radio-overseer panel on the RADIO tab renders ONLY in dev mode (renderRadioOverseer, fed by renderRadioTab; switchMainTab('radio') now calls renderRadioTab so the desk appears the moment dev mode arms): per-dial ON AIR . TRK n/N (computed live locally) or OFF AIR + [GO LIVE] (confirm-gated, epoch=serverNow = everyone starts track 1 together) / [SKIP] (epoch bumped to serverNow minus cum-duration-through-current-track, every unit jumps together, wraps the loop) / [CUT] (confirm-gated firebaseRemove). Dial sync is INDEPENDENT per station (any mix of live and free-run) per user sign-off in discussion. Downloader, RADIO_CACHE bucket, CDN host and all 90 track files UNCHANGED. Legacy note: pre-v0.54 units ignore the radio/ node entirely (free-run while synced units share) -- update all units before the event. Cache bumped to pipboy-cache-v43 (fresh duration-bearing manifest rides the precache refresh). Ella Fitzgerald "Into You" title mapping CONFIRMED CORRECT by user -- flag closed.
*   **v0.55:** FIELD POLISH BATCH (user discussion sweep, 8 items, NO Firebase rules changes). MAP CAMERA DE-FORCED (user: "map opens forced and zoomed on location"): two yankers removed -- ensureUserMarker's setView(zoom 16) on first dot-draw (the jump-to-your-face every map open) and renderWaypoints' fitBounds on every marker-list re-render. The map now RESTORES the last view you left: initPipMap reads pipboy-mapview {c:[lat,lng], z} (written on every moveend) and falls back to the old waypoint-frame/Perth init only on first-ever open. New all-player camera row under the map (#map-cam-controls, gated in switchMainTab tabId==='map', NOT dev-gated): [O ME] mapGoMe() (zoom keep-or-16, guards no-fix) + [FIT ALL] mapFitAll() (frames own waypoints + shared pins + own fix via L.latLngBounds().pad(0.25)). ADJUSTABLE ZONE SIZES (user): radius is no longer hard-wired 15 -- dropHotZone/dropMedZone are thin wrappers into new promptZoneRadius(kind, where), a themed showCustomPrompt offering 10/15/25/50/100M FENCE chips (kind-tinted) then dropZone(kind, lat, lng, radius) (radius param, clamped 5..200 -- the rules validator has accepted that range since v0.50, so NO rules trip); deploy notify reports the fence size; Overseer panel section renamed from ZONES (STATIC, 15M) to ZONES. SELECT SWEEP (user: "the select to remove markers is still generic -- check every one"): all LIVE native <select> dropdowns purged (closed state was themed, the OPEN popup was OS-white): (1) REMOVE MARKER -- the whole #remove-waypoint-modal is DELETED from index.html; openRemoveWaypointModal() is now a themed picker of your markers (each with a confirm gate) routing to deleteWaypointById(id) (old select-reading deleteWaypoint() gone); (2) quest-composer TYPE + GIVEN BY -- selects became readonly pip-inputs tapping to pickQuestType()/pickQuestGiver() (giver lists live factions + UNKNOWN WASTELANDER); values ride new qTypeVal/qGiverVal state vars, consumed by saveNewQuest (stored semantics unchanged: MAIN/SIDE codes); openAddQuestModal resets both instead of filling <option>s; (3) faction-editor SELECT FACTION -- became a readonly input tapping to pickFactionToEdit() riding new facEditId state; the AUTH EDIT/EDIT_SPECIFIC branch, populateEditFaction, saveEditFaction and deleteFaction all read facEditId now (delete guards a null pick). The one surviving <select> (#add-type) sits in the v0.53-inert item modal with zero UI paths -- inert by policy. MAIL ICON (user: "new mail icon next to mail heading?"): renderMailBadge now renders a PULSING AMBER ✉ (new .mail-pip CSS: color/glow + mailPipPulse keyframes, 1.6s) before the MAIL counts whenever n+u+k > 0, plain MAIL otherwise; it also drives the installed-PWA home-screen icon badge via navigator.setAppBadge(n+u+k)/clearAppBadge (guarded, try/caught, unsupported browsers skip silently). MAIL DELIVERY HARDENING (user: "quest took 7 minutes, is this normal?" -- answer: SENDER-side queue, not the satellite): flush triggers lacked any wake path, so an Android-frozen tab sat on its letters until the 20s interval next ran live; added flushOutbox()+refreshOutboxStatuses() on BOTH visibilitychange(visible) and pageshow alongside the existing 'online' listener + 20s heartbeat; the outbox 'sending' status now reads "QUEUED ON THIS UNIT -- SHIPS WHEN OPEN IN SIGNAL" so senders see ownership (a letter in that state hasn't left THEIR phone yet; once it lands, receivers get it in seconds). ACTION REQUIRED RED (user): the mail feed's zone-1 header is now blood-red (#ff3333 text + glow + border). ISSUE QUEST FROM CONTRACTS (folded in from queue): DATA > CONTRACTS gains [+ ISSUE NEW QUEST] (dev-mode-only, rendered inside renderContracts so it re-gates immediately; switchMainTab('data') now also re-renders contracts) -> issueQuestStart() -> openRecipientPicker('quest') (picker parametrized: msg keeps "SELECT RECIPIENT:" + ✉ labels, quest gets "ISSUE CONTRACT TO:" + ☢ labels) -> composeTo('quest', uid) (link-gated as before) -> the existing quest composer. MAIL PUSH ANSWER (user: "can ONLY mail push to the phone?"): it already does -- v0.48 decoupled showNotification to strictly in-app; OS pushes flow ONLY via mailPingOs() on transmission arrivals, gated by the MAIL PING master + per-category NOTIFY switches, silent while reading the MAIL tab (the new icon badge above complements it). No new push code shipped this deploy beyond the badge. Cache bumped to pipboy-cache-v44.
*   **v0.56:** COMMUNICATIONS & MAP HARDENING MEGA-BATCH (11-spec batch from user field reports + confirmations). *** FIREBASE RULES MUST BE REPUBLISHED: (a) new `announcements` node -- {".read": true, "latest": {".write": true, hasChildren(['text','from','ts']), text 1..140 chars, from <=32, ts number >1735689600000, $other:false}, "$other":{".validate":false}} single-slot design (each wire overwrites the last); (b) sharedpins gains an `icon` validator (string <=24) reserving the key for future location-art -- same self-police trust model, dev-mode PIN gated writes, no auth. Paste BEFORE pushing files (order is safety only -- old units ignore the node, new units' wire writes just fail until the rules land). *** (1) BEACON BLACKOUT FIX (user: \".55 unit sees roster+zones but NO dots\"): root-cause hunt found two killers -- (a) one rotten beacon record (string coords, missing fields) could throw mid-render-loop and black out EVERY dot on the unit (roster/zones painted by separate listeners kept working) -- listener now parseFloat+isFinite plates every coord and try/catches per-marker so a bad record costs ONE dot; (b) sleeping senders: beacons keepalive-die after 5min and go COLD -- cold dots now draw GHOSTS (dashed dim, no glow) instead of impersonating live ones; new SIGNALS census chip top-left of the map (#map-signals, \"SIGNALS: n LIVE . n COLD\") answers \"is the map empty or is everyone asleep\" at a glance; WAKE-STAMP added -- visibilitychange/pageshow now immediately re-push your beacon (live-tracking opted-in only, has-fix only) and re-run the rad/pariah field math, alongside v0.55's outbox flush. (2) PIN TAP-REVEAL + OVERSEER REMOVAL (user: \"pins should only show names when clicked\"): shared pins + own waypoints tooltips now permanent:false (name fades until tapped); selectSharedPin(key) pins a card (label/VIA/distance, deselectPin on map tap or other select); dev mode adds a red [REMOVE BROADCAST] -> confirm -> firebaseRemove sharedpins/key. (3) ACK-KILL (user: \"no need to log acknowledgement of message - reply is fine\"): outbox claimed-branch notify now gated `e.type !== 'msg'` -- plain letters no longer ping the sender \"TRANSMISSION ACCEPTED BY\", replies remain the ack; contracts/quests still confirm. (4) SAVE PHOTO (user): [SAVE PHOTO] button on incoming photo letters + feed full-view -> saveMailPhoto(dataUrl) unshifts into photoArchive (string entries are first-class via entryPip/entryRaw helpers), persists pipboy-photos, gallery refresh. (5) QUICK-CAM (user: \"take picture from within message when sending\"): [SNAP NOW] button on the composer attach row opens #quickcam-modal (getUserMedia, facingMode from currentFacingMode, [SNAP + ATTACH] -> hidden canvas, 1024px-max JPEG 0.72 -> cmAttach.photo string -> back to composer). (6) HUD GLYPH STRIP + TOAST CULL (user: \"easy up on acknowledgements esp radio/gps; a symbol in top bar like mobile signal\"): #hud-sat (off=dim/locked=amber/UNSTABLE=red-blink via lastFixAt>90s + .hud-blink keyframes) + #hud-radio (hidden/green free-run/amber synced) sit in the top bar, updated from gpsOnFix/stopGpsWatch/renderRadioTab/15s health tick/boot; CULLED toasts: SATELLITE LINK RESTORED, TUNED TO LIVE BROADCAST, STATION SYNC ON/OFF, OVERSEER SHIFTED (glyph carries state); KEPT: GPS denial, BROADCAST ENDED, mail arrivals, download failures, SYNC LOCKED refusal. (7) PERSISTENT SELF-DOT (user: \"always display own marker, always\"): gpsOnFix saves pipboy-lastfix {lat,lng,ts}; hydrateLastFix() at boot + initPipMap restore branch (no live fix yet) draw a GHOST self-dot (dashed -- \"last known me\"); stopGpsWatch now demotes to ghost instead of removing the dot; ensureUserMarker(lat,lng,cold) third param restyles live<->cold in place. (8) ZONE RADIUS ENGINE (user's rads mystery: \"removed the med zone, he placed a series of rad zones, no damage from any source\"): the v0.55 pickable 10/15/25/50/100M radii previously changed only RENDERING -- the tick engine still bit at a fixed 15m; now z.radius feeds grab-threshold r with a release hysteresis of r*1.2 (legacy 15m zone = identical 15/18 behaviour; pariah fields carry no radius and stay legacy 15); STAT tab gains a FIELD STATUS line (INSIDE zone-name / IN MED SHELTER / OPEN WASTES + last-fix age, colored). NB: the mystery tester's unit is also suspected of running a PRE-v0.50 build (no zone engine at all) -- read the version header on that unit before chasing further. (9) SEEDED RADIO SHUFFLE (user: \"randomise playlist order\"): radioOrder map + radioSeededOrder(st,seed) mulberry32; SYNCED dials derive order from the desk's epoch -- every unit shuffles identically so \"same track same second\" survives; FREE-RUN dials seed randomly per shuffle; radioTrackIdx is now an ORDER POSITION everywhere (radioTrackAt translates), persisted radioPos stores the underlying TRACK index so resumes survive reshuffles; overseer SKIP walks the shared order. Verified: same seed identical / different seed differs / valid permutation / 5 sequential epochs unique. (10) OVERSEER WIRE -- ANNOUNCEMENT BROADCAST (user: \"a way to broadcast announcement as overseer\"; shipped single-slot per discussion, true OS push parked to Tier 2): STAT > STATS \u2265 WIRE DESK (dev-only, #wire-text vk-target + [TRANSMIT WIRE], <=140 chars, uppercase, confirm-gated) writes announcements/latest {text, from, ts}; startWireListener (piggybacks the radio db-ready branch, skip-first-snapshot) -> fresh wire = in-app showNotification (gated by new OPTIONS [WIRE ALERTS: ON/OFF], default ON, pipboy-wire-alerts) + pushNativeNotification attempt (#wire-banner dismissible strip above the nav tabs shows the live wire, 12h expiry, dismiss persisted per-ts via pipboy-wire-dismissed; boot re-renders the last wire from pipboy-wire-last). Legacy note: pre-v0.56 units never see wires. (11) MAIL LABEL + misc: [SAVE PHOTO] copy sits beside the v0.55 actions; OPTIONS explainer extended one line. index.html 3 tags + splash -> v0.56. Cache bumped to pipboy-cache-v45, RADIO_CACHE stays 'pox-radio-v1'.
*   **v0.57:** ZONE HOTFIX BATCH (4 user field notes after v0.56, NO Firebase rules changes). ANY RADIUS (user: "Hot/med zone set any radius"): the 5-chip picker stays for speed, gains a sixth [SET RANGE...] chip -> promptZoneStepper(kind, where, lat, lng) -- a TAP-STEPPER row inside the same themed prompt (NO native keyboard, same convention as the faction-auth amount): [−25][−5][−1][ NM ][+1][+5][+25], clamped 5..200M (the rules validator has allowed that range since v0.50), starting at 15M, with a glowing live readout; the DEPLOY button label rewrites itself per tap ("DEPLOY 35M FENCE") via new optional button.id support in the prompt renderer; [BACK TO QUICK SIZES] loops to the chip picker preserving kind+where. MECHANISM: showCustomPrompt(text, buttons) gained an optional 3rd config {value,min,max} -> renders into new #cp-stepper div in the custom-prompt modal (hidden/reset on every open so old steppers never bleed into unrelated prompts); pending number rides new cpStepVal state, read by the deploy action at commit time. Generic on purpose -- bounty payout dials etc. reuse it later. DEPLOY-ACK CULL (user: "dont need ack after deployment"): dropZone success .then(showNotification "... ZONE DEPLOYED (nM FENCE).") DELETED -- the fence drawing itself is the receipt; .catch failure notice DEPLOY FAILED -- CHECK SIGNAL OR RULES stays (silence must never read as "maybe it worked"). MODAL STAYS OPEN FIX (user: "after deploying hot/med zone the add marker UI stays up"): root cause -- the map placement flow (long-press -> #add-waypoint-modal -> [HOT/MED ZONE HERE] -> promptZoneRadius -> chip picker) never closed the modal, so after deploy it popped back up from under the picker; promptZoneRadius now force-hides #add-waypoint-modal on entry (no-op for the drop-at-my-boots stats path). Also snipped the stale "(15M)" off both zone buttons' labels -- radius has been operator-picked since v0.55. HUD STRIP HORIZONTAL (user: "gps sig/radio si can go across horizontally, currently it makes the bar expand vertically"): root cause -- the header's left block is a flex COLUMN, so clock/♪/⌖/batt each stacked their own line and the header grew up to 4 rows tall; all four now ride ONE flex row under the title (.gap 8px, nowrap, mobile-signal style) and the header caps at 2 lines total; per-span margin hacks stripped. updateHud untouched (it only writes style/color/display). index.html 3 tags + splash -> v0.57. Cache bumped to pipboy-cache-v46, RADIO_CACHE stays 'pox-radio-v1'.

*   **v0.58:** UI POLISH BATCH (NO Firebase rules changes). ZONE STEPPER: promptZoneRadius now offers ±25/±5/±1 tap-stepper for any radius 5–200M (was chip-only). DEPLOY-ACK CULLED: zone deploy success toast removed (fence drawing is the receipt). ADD MARKER MODAL CLOSE: dropZone now calls closeModals() before Firebase write. HUD STRIP HORIZONTAL: PWR/⌖/♪ moved to top-right of header in flex row. SNAP NOW REMOVED: quickcam button + modal deleted from MAIL composer. MAP CAMERA OVERLAY: ◎ and ⛦ as compact 36px glyphs positioned bottom-right on map surface (was footer buttons). REMOVE MARKER SCROLL: showCustomPrompt scrollTop=0 added. RADIATION LEVEL ROW REMOVED: redundant with HP/RADS bar. SPECIAL/SKILLS/PERKS MERGED: one scrollable sub-tab. OPTIONS MOVED: from DATA to STAT sub-tab. NUKA-COLAS REMOVED: from general stats. WILD STATS ADDED: total rads absorbed, distance travelled, peanuts stolen, times near death, photographs regretted, enclave marches endured. Cache bumped to pipboy-cache-v47, RADIO_CACHE stays 'pox-radio-v1'.
*   **v0.59:** MUTATIONS + DECON + STARCHED GENES + OVERSEER TAB + ONBOARDING REWORK. *** FIREBASE RULES MUST BE REPUBLISHED: (a) radzones kind validator now accepts 'hot'|'med'|'decon'; (b) wastelanders gains mutations numeric field (0–20); (c) new world node with starchedUnlocked boolean. *** MUTATION SYSTEM: 11 mutations (EGG HEAD, MARSUPIAL, SCALY, ADRENAL, EAGLE EYES, THICK SKIN, GLOW BLOOD, CARNIVORE, HERBIVORE, NIGHTKIN, WASTELAND WHISPERER). Trigger: current rads ≥ 250 → 10% roll per 5s tick in hot zones or pariah fields. 10-minute cooldown between gains. No cap (max 10 active due to CARNIVORE/HERBIVORE clash pair). Stat effects: +2 buff / −2 debuff to S.P.E.C.I.A.L., applied via recomputeSpecial() from baseSpecial snapshot. Beacon broadcast: mutation count rides beacon data, orange ☢N indicator on map cards. Mutations list rendered in S.P.E.C.I.A.L. sub-tab below perks. DECONTAMINATION STATIONS: new zone kind 'decon' in radzones node. Fixed 15m radius, Overseer-deployable. Once per entry: sheds ALL rads instantly + 50% chance to strip random mutation. Hysteresis: 15m grab / 18m release. Visual: cyan dashed ring with ✦ diamond. STARCHED GENES: perk that locks mutations permanently. Overseer global toggle via world/starchedUnlocked Firebase boolean. Player toggle button in STATS panel when globally enabled. One-time unlock stored in pipboy-starched localStorage. Visual: 🧬🔒 DNA strand icon next to vault-boy portrait when active. OVERSEER TAB: new STAT sub-tab consolidating all Overseer controls (pariah watch, zones, Starched Genes toggle, wire desk). Dev-gated — nav item hidden for regular players. Radio stays on RADIO tab. HEADER RESTRUCTURE: PWR/⌖/♪ moved to top-right of header in horizontal flex row. Title + version on left, status glyphs on right, clock below. Header caps at 2 lines max. MAP CHANGES: overlay camera buttons (◎ ⛦), footer map buttons removed, zone stepper, zone deploy ack culled, add marker modal closes after zone deployment. MAIL CHANGES: SCAN TRANSFER removed, SEND NEW MESSAGE full-width. STAT TAB RESTRUCTURE: SPECIAL/SKILLS/PERKS merged, OPTIONS moved to STAT, mutations section added, wild stats added, redundant stats removed, RADIATION LEVEL row removed. ONBOARDING REWORK: origins pure bonuses (no penalties), item grants removed (INV retired since v0.53), Guzzoline Addict: drink tickets removed, HP=80 kept, half-rad mechanic added (50% skip per rad tick). Wasteland Legend (was Lord of the Wasteland): drink tickets → +20 rep ALL factions. Cache bumped to pipboy-cache-v48, RADIO_CACHE stays 'pox-radio-v1'.
*   **v0.60:** BUG FIX BATCH (NO Firebase rules changes). REMOVE MARKER SCROLL FIX: custom prompt modal changed to position:fixed with full-height scroll, max-height constraints removed from modal content and button container. LEGACY MARKERS: DEFAULT_WAYPOINTS array + WAYPOINT_VERSION constant; new defaults merge into existing users' lists on app load (bump version to add new defaults in future). TOUCH SCROLL FIX: added -webkit-overflow-scrolling:touch and touch-action:pan-y to custom prompt modal overlay and content. RADIO OVERSEER ACK CULLED: GO LIVE / SKIP / CUT no longer toast notifications. Cache bumped to pipboy-cache-v49, RADIO_CACHE stays 'pox-radio-v1'.
*   **v0.61:** STARCHED GENES TOGGLE + REPLY LOGGING + SCROLL CSS FIX + KEYBOARD BRACKETS + CANCEL QUEST (NO Firebase rules changes). STARCHED GENES TOGGLE: changed from one-time unlock to player toggle (ON/OFF) in STATS panel. When Overseer disables globally, all players who had it unlocked lose it automatically (listener clears pipboy-starched on global disable). REPLY LOGGING FIXED: added renderMail() call after saveComms() in transmitMsg() so replies appear immediately in mail feed. REMOVE MARKER SCROLL CSS FIX: added explicit user-select:auto !important and touch-action:pan-y !important overrides on modal overlay, content, and button container to override global user-select:none that was blocking touch scrolling on mobile. Buttons themselves keep user-select:none to prevent text selection on tap. KEYBOARD BRACKETS: added [ ] < > ( ) / ! row to virtual keyboard below punctuation row. CANCEL ISSUED QUEST: CONTRACTS tab now shows red [CANCEL] button for active contracts (sent/awaiting/sending). Tapping shows confirmation prompt, then removes quest from outbox and deletes Firebase mail letter via clearOutboxEntry(). Cache bumped to pipboy-cache-v50, RADIO_CACHE stays 'pox-radio-v1'.

*   **v0.62:** REPLY AUTO-LOG + MAIL BADGE LAYOUT + REMOVE MARKER SCROLL FIX (NO Firebase rules changes). REPLY AUTO-LOG: when replying to a message from ACTION REQUIRED, the reply now automatically logs the original message (removes it from ACTION REQUIRED). Implemented by tracking replyToKey through composeTo() and calling acceptMsg() in transmitMsg() when replyToKey is set. MAIL BADGE LAYOUT: added flex-wrap:nowrap and min-width:80px to #mail-navitem to prevent the (1) badge from causing the MAIL tab to drop/shift position. REMOVE MARKER SCROLL FIX: removed maxHeight:65vh constraint on #cp-buttons container and added 50ms delayed scroll reset (setTimeout) to ensure layout is complete before scrolling to top. This fixes the issue where 9+ markers would hide the first marker under the UI. Cache bumped to pipboy-cache-v51, RADIO_CACHE stays 'pox-radio-v1'.

*   **v0.63–v0.75:** (INTERIM VERSIONS — detailed changelogs not captured in this handoff. Key features added: Glowing Ones transformation at 1000 rads, photo evidence for contracts, overseer contracts-to-verify panel, bounty surrender codes. User reverted to v0.76 due to popup issues in v0.77-v0.83.)
*   **v0.76:** STABLE BASELINE. User confirmed working with no popup issues. This version served as the rollback target during v0.77-v0.83 development.
*   **v0.77–v0.83:** (UNSTABLE — popup issues caused user to revert to v0.76. Features developed during this period were re-implemented in v0.84.)
*   **v0.84:** FEATURE RE-IMPLEMENTATION (all features from v0.77-v0.83 re-built on v0.76 baseline). GAP FIX: removed 2px margin-top on wastelanders-list-map. TAB HEADINGS: changed nav-item from flex:1 to flex:0 0 auto for even spacing. SCROLL FIX: modal-content max-height 90vh + overflow-y:auto. PHOTO EVIDENCE: global contracts can attach photo evidence (stored in globalContracts/{id}.evidencePhoto). OVERSEER CONTRACTS TO VERIFY: new section in OVERSEER tab showing completed contracts awaiting verification with verify/reject buttons. BOUNTY SURRENDER CODES: 6-digit codes (5-minute expiry) replace QR scanning — target generates code, hunter enters code. New Firebase node: surrenderCodes/{uid} {code, generatedAt}. Cache bumped to pipboy-cache-v73.
*   **v0.85:** SYSTEM QUERY POPUP FIX. Root cause: #custom-prompt-modal CSS had display:flex!important which overrode .modal-overlay's display:none, making the modal always visible on page load. Fix: removed display:flex!important from base rule, added #custom-prompt-modal.active { display:flex!important } and JS toggles the active class. Created closeCustomPrompt() helper. Cache bumped to pipboy-cache-v74.
*   **v0.86:** CONTRACT VISUAL FEEDBACK + OVERSEER AUTO-LOAD + PHOTO VIEWING + MARKER SCROLL. renderGlobalContracts() now shows ALL contracts (not just open) with strikethrough + coloured borders: completed=amber, verified=green, rejected=red. Overseer contracts-to-verify auto-loads on tab render. Evidence photos viewable in contract modal. Remove marker heading text removed to fix scroll. Cache bumped to pipboy-cache-v75.
*   **v0.87:** CONTRACT COMPLETION FIX + FIREBASE RULES. Root cause: completeGlobalContract() was setting verifiedBy=null which failed Firebase string validation. Removed the null assignment. Firebase rules updated: globalContracts gains evidencePhoto field, completedBy changed to .validate:true (allows arrays), status gains 'rejected'. Cache bumped to pipboy-cache-v76.
*   **v0.88:** NESTED SCROLL FIX. Buttons container (#cp-buttons) had its own scroll (maxHeight:65vh/85vh) inside modal-content which also scrolled. Removed buttons container scroll — modal-content is now the single scroll container. Cache bumped to pipboy-cache-v77.
*   **v0.89:** VAULT BOY IMAGE MAXIMIZED + ACTION POINTS REMOVED. Vault boy image changed from fixed 180×180px to width:100%; max-width:280px; aspect-ratio:1/1. Container changed from height:200px to min-height:200px. Action Points row deleted from stat page (was dead code, never referenced in JS). Cache bumped to pipboy-cache-v78.
*   **v0.90:** AP FOOTER REMOVED. Deleted the footer-ap-display "AP 100/100" block and its bar from footer HTML (dead code, never updated by JS). Cache bumped to pipboy-cache-v79.
*   **v0.91:** UNIFIED QUEST SYSTEM — COMPLETE REBUILD. *** FIREBASE RULES MUST BE REPUBLISHED: new quests/ node with progress/{uid} sub-nodes. *** Three quest types: DIRECT (sent to specific person via mail), GLOBAL (visible to all, accept to add to your quests), BOUNTY (hunt a target, scan their datacard to complete). Anyone can create any type. Issuer verifies their own quests. Overseer can verify anyone's quests. QUESTS tab sub-tabs: ACTIVE (your accepted quests) / AVAILABLE (open globals + bounties) / ISSUED (your created quests with pending verifications). Bounty completion: hunter scans target's normal datacard QR → auto-completes if uid matches bounty target. Evidence photos: attach before completing, issuer views photo before verifying. Push notifications: completing a quest sends verify-request mail to issuer; creating a direct quest sends quest-offer mail to recipient. DELETED: all globalContracts code, all bounties code, surrender code system, overseer contracts-to-verify panel. ADDED: Firebase imports push and update. Firebase rules: new quests/ node with validators for all fields + progress/{uid} sub-node. Cache bumped to pipboy-cache-v80.
*   **v0.92:** SCROLL FIX ATTEMPT. Added explicit max-height:none; overflow-y:visible to #custom-prompt-modal .modal-content to override base .modal-content class's max-height:90vh; overflow-y:auto (was creating nested scroll). Added 50ms setTimeout before scrollTop=0. Cache bumped to pipboy-cache-v81.
*   **v0.93:** SCROLL FIX — AGGRESSIVE APPROACH. Added explicit width:100vw; height:100vh to overlay. Changed JS scroll reset from setTimeout to double requestAnimationFrame for better layout timing. Added both scrollTop=0 and scrollTo(0,0) for cross-browser compatibility. Cache bumped to pipboy-cache-v82.

*   **v0.94:** SCROLL FIX — TRIPLE-TAP APPROACH + HANDOFF UPDATE. Scroll reset now happens BEFORE showing the modal (prevents inherited scroll position), then immediately after, then after two requestAnimationFrame cycles. AGENT_HANDOFF_INSTRUCTIONS.md updated with missing changelog entries v0.63-v0.93. Cache bumped to pipboy-cache-v83.
*   **v0.95:** BLANK SCREEN FIX. Root cause: renderActiveQuests() was calling `firebaseQuests.forEach()` but `firebaseQuests` is an object (`{}`), not an array — objects don't have `.forEach()`. This crashed the app on load. Fix: changed to `quests.forEach()` for the legacy local quests array (from localStorage). Cache bumped to pipboy-cache-v84.
*   **v0.96:** BLANK SCREEN CRASH FIX + VERSION DISPLAYS. Root cause: `renderQuests()` function was missing — called at startup (line 7469) and in 14+ other places throughout the code, but never defined after the quest system rebuild in v0.91. This threw `ReferenceError: renderQuests is not defined` and crashed the app immediately on load, leaving only the CRT glow visible. Fix: Added `renderQuests()` wrapper function that detects the active quest sub-tab and calls the appropriate new render function (`renderActiveQuests`/`renderAvailableQuests`/`renderIssuedQuests`). VERSION DISPLAYS: Updated two hardcoded v0.84 version tags on the boot splash screen and pre-boot calibration overlay to v0.96. Cache bumped to pipboy-cache-v85.


## 7. Deployment & Testing Guide

**Standard deployment process:**
1. Update version number in `index.html` (header line ~60, boot splash screen, pre-boot overlay)
2. Update `CACHE_NAME` in `sw.js` (increment the number, e.g., `pipboy-cache-v85` → `pipboy-cache-v86`)
3. Test locally if possible
4. Create zip: `cd pwa && zip -r ../Pox-Boy-3026-vX.XX.zip . -x ".git/*" "node_modules/*"`
5. Commit and push to Git (triggers Netlify auto-deploy)
6. Hard refresh on test devices to clear service worker cache
7. Update this document's changelog

**Testing on Netlify Drop (temp deployments):**
- ✅ **Works on drop:** UI changes, CSS fixes, local storage features (items, stats, mutations, S.P.E.C.I.A.L.), JavaScript bug fixes, virtual keyboard, PWA install
- ❌ **Requires production deployment:** Firebase features (quests, map beacons, mail, global contracts, bounties, shared pins, pariahs, rad zones, radio sync, announcements) — Firebase blocks connections from unauthorized domains like `random-name-12345.netlify.app`
- **Rule of thumb:** If the feature uses `firebaseRef()`, `firebaseOnValue()`, `firebaseSet()`, `firebaseUpdate()`, or `firebaseRemove()`, it needs production deployment to test
- **When in doubt:** Check the deployment report — if it mentions "Firebase rules updated" or "new Firebase node", it needs production deployment

**Quick reference for common features:**
| Feature | Works on Drop? | Notes |
|---------|----------------|-------|
| Items/Inventory | ✅ | Local storage only |
| Stats/S.P.E.C.I.A.L. | ✅ | Local storage only |
| Mutations | ✅ | Local storage only |
| Factions | ✅ | Local storage only |
| Waypoints | ⚠️ Partial | Local waypoints work; shared pins need Firebase |
| Map | ⚠️ Partial | Map renders, but no live beacons without Firebase |
| Quests (local) | ✅ | Old localStorage quests work |
| Quests (unified v0.91+) | ❌ | Requires Firebase `quests/` node |
| Mail/P2P comms | ❌ | Requires Firebase `mail/` node |
| Camera/Databank | ✅ | Local storage only |
| Radio | ⚠️ Partial | Local playback works; sync needs Firebase |
| Overseer features | ❌ | Most require Firebase |

*   **v0.97:** QUEST CREATION FORM FIX. Root cause: `createQuestForm()` was building HTML as a string and passing it to `showCustomPrompt(html, [])`, which treats the first parameter as plain text and displays the raw HTML instead of rendering it. Fix: Created dedicated `create-quest-modal` in index.html with proper form fields (title, description, reward, recipient/target dropdowns). Updated `createQuestForm()` to show the modal, populate dropdowns based on quest type, and store pending quest type. Added `submitQuestFromModal()` to read form values and call `submitQuest()`. Removed `closeCustomPrompt()` call from submitQuest since modal is now closed via `closeModals()`. Cache bumped to pipboy-cache-v86.

*   **v0.98:** QUEST CREATION PERMISSIONS. Updated `createQuestForm()` to enforce permission rules: (1) **Direct quests** can only be issued to mutual contacts (2-way handshake confirmed) — dropdown filters rolodex via `isMutualLink(uid)`, shows "No mutual contacts yet" if empty. (2) **Bounties** can be placed on anyone — dropdown shows all rolodex contacts PLUS all known wastelanders from `lastKnownBeaconData` (map signals), labeled as "(contact)" or "(signal)". (3) **Global quests** remain open to anyone (no recipient/target selection). Cache bumped to pipboy-cache-v87.

*   **v0.99:** QUEST PICKER UI FIX — replaced native Android select dropdowns with themed in-app pickers. Root cause: `<select>` elements show native OS UI on mobile, breaking PIP-Boy immersion. Fix: Replaced select elements with readonly `<input>` fields + hidden value fields. Added `openQuestRecipientPicker()` (shows mutual contacts only via `isMutualLink()` filter) and `openQuestTargetPicker()` (shows all rolodex contacts + beacon signals). Both use `showCustomPrompt()` with button arrays for themed UI. Updated `submitQuestFromModal()` and `submitQuest()` to read from hidden fields instead of select options. Cache bumped to pipboy-cache-v88.

*   **v0.100:** SIX-FIX BATCH — ISSUED QUEST MODAL + ACCEPT QUEST + HANDSHAKE + CONTRACTS TAB + HEADING SPACING + MAIL BADGE. (1) **ISSUED QUEST MODAL:** `openIssuedQuestModal()` was passing HTML to `showCustomPrompt()` which treated it as plain text. Replaced HTML string with plain text formatting, moved VERIFY/REJECT buttons into the buttons array. (2) **ACCEPT QUEST:** Added `renderActiveQuests()` call after `switchQuestTab('active')` in `acceptQuest()` to ensure the active quests tab refreshes immediately. (3) **HANDSHAKE TWO-WAY:** Root cause: `isMutualLink()` checked for `status === 'accepted'` but accepted handshakes are marked as 'closed' (receiver retired the letter). Fixed by adding `|| e.status === 'closed'` to the mutual link check. (4) **CONTRACTS TAB REMOVED:** Removed DATA > CONTRACTS sub-tab and `sub-data-contracts` div — redundant with QUESTS > ISSUED tab. (5) **HEADING SPACING:** Added `padding: 8px 12px; margin: 0 4px; display: inline-block;` to `.sub-nav-item` CSS for better spacing between tab headings. (6) **MAIL BADGE SIMPLIFIED:** Removed complex count display `(n) (u?) (k LINKS)` from `renderMailBadge()`. Now just shows amber envelope icon when there are items, plain text when quiet — better for dynamic spacing/scaling. Cache bumped to pipboy-cache-v89.

*   **v0.101:** RADIO STATIONS INTEGRATION — added W.A.S.T.E FM and Unknown Transmission stations from Archive.org. W.A.S.T.E FM: 11 tracks (all Slayer "Raining Blood" variations, 19.9M total) from https://archive.org/details/waste_202608. Unknown Transmission: 16 tracks (nightcore versions, 21.4M total) from https://archive.org/details/unknown_20260817_2203. Updated `radio-stations.json` with proper base URLs (`https://archive.org/download/waste_202608` and `https://archive.org/download/unknown_20260817_2203/Unknown`), track metadata (title, artist, filename, bytes, estimated duration), and file structure. Radio tab now shows all 5 stations (GNR, DCR, APPA, W.A.S.T.E, Unknown). Cache bumped to pipboy-cache-v90.

*   **v0.103:** RADIO URL FIX — updated W.A.S.T.E FM and Unknown Transmission base URLs from `pox-radio.netlify.app` to `pox-alt-radio.netlify.app` (user deployed to alt site). Radio tab now streams from correct Netlify site with CORS headers. Cache bumped to pipboy-cache-v92.

*   **v0.104:** RADIO FILENAME FIX — corrected 4 track filenames with double spaces to match actual files on Netlify. Fixed: "Raining Blood  Slayer, Orchestral Arrangement no 2.opus", "Slayer - Raining Blood (Dark Viking Doom Cover)  Bardcore Northblood.opus", "Slayer's Raining Blood But it Grooves_ YOU WON'T BELIEVE IT! (AI HEAVY  FUNK COVER).opus", "ROY BEE  - Kiss Me Again (Nightcore Official Mix).opus". All 27 tracks now download successfully from pox-alt-radio.netlify.app. Cache bumped to pipboy-cache-v93.

*   **v0.105:** RADIO SYNC IMPROVEMENTS PHASE 1+2 — tighter sync tolerance (0.8s vs 1.5s), faster watchdog (3s vs 5s), visual sync indicator (±Xs with color coding), manual ±0.1s local adjustment buttons, predictive buffering (pre-loads next track 8s before end), and crossfade transitions (1.5s fade between tracks in free-run mode). Added `radioLocalOffset` variable with localStorage persistence (`pipboy-radio-offset`). Added `radioNextTrackAudio` and `radioCrossfadeAudio` for dual-audio crossfade. Sync indicator shows green (<1s), amber (1-2s), red (>2s) with live drift updates. Cache bumped to pipboy-cache-v94.

*   **v0.106:** QUEST SYSTEM FIXES + MAP AUTO-CENTER. (1) **CANCELLED QUEST STRIKETHROUGH:** Updated `renderIssuedQuests()` to apply `text-decoration: line-through` and `opacity: 0.5` to cancelled quests, with dimmed border color. (2) **MAP TAB AUTO-CENTER:** Modified `switchMainTab('map')` to call `mapGoMe()` 100ms after map initialization, automatically centering on user's location when MAP tab is opened. (3) **QUEST ACCEPT/CLICK VERIFICATION:** Confirmed `acceptQuest()` and `openQuestModal()` functions are working correctly — if quests don't accept or modals don't open, it's likely due to Firebase being blocked on Netlify Drop (temp deployment). Requires production deployment for full quest functionality. Cache bumped to pipboy-cache-v95.

*   **v0.107:** OVERSEER QUEST REMOVAL + CANCEL IMPROVEMENTS. (1) **OVERSEER REMOVE QUEST:** Added `removeQuest()` and `executeRemoveQuest()` functions allowing Overseer (dev mode) to remove any quest with a reason (DUPLICATE/INAPPROPRIATE/TESTING/OTHER). Removed quests stored with `status: 'removed'`, `removedReason`, `removedAt`, `removedBy` fields. (2) **CANCEL ACCEPTED QUESTS:** Modified `cancelQuest()` to allow issuers to cancel quests even if they have progress, with warning dialog. (3) **REMOVED QUEST DISPLAY:** Updated `renderIssuedQuests()` to display removed quests with red border, strikethrough, 0.4 opacity, and removal reason/byline. (4) **ISSUED QUEST MODAL BUTTONS:** Updated `openIssuedQuestModal()` to show CANCEL button for accepted quests (with warning) and REMOVE button for Overseer on all non-removed quests. Cache bumped to pipboy-cache-v96.

*   **v0.108:** FIREBASE RULES FIX + UI CLEANUP + QUEST MODAL ERROR HANDLING. (1) **FIREBASE RULES:** Added 'removed' to allowed status values in quests node. Added validators for removedReason (string <=100), removedAt (number/null), removedBy (string <=32) fields. (2) **UI CLEANUP:** Removed standalone [+ CREATE QUEST] button from QUESTS tab (users can still create quests via the ISSUED tab). (3) **QUEST MODAL ERROR HANDLING:** Updated `openQuestModal()` to show "QUEST NOT FOUND" notification if quest doesn't exist in firebaseQuests (helps debug Firebase listener issues). Cache bumped to pipboy-cache-v97.

*   **v0.109:** CRITICAL FIX - BLANK SCREEN. Root cause: Python script duplicated error handling code in `openQuestModal()` function, causing "Identifier 'q' has already been declared" syntax error that prevented app from loading. Removed duplicate lines 2121-2125. Cache bumped to pipboy-cache-v98.

*   **v0.110:** QUEST BUTTON FIX + QUEST MODAL ERROR HANDLING. (1) **BUTTON FIX:** Restored NEW [+ CREATE QUEST] button (unified quest system with type selection) in QUESTS tab. Removed OLD [+ ADD QUEST] button (legacy quest system) from bottom bar. (2) **QUEST MODAL ERROR HANDLING:** Added try-catch wrapper to `openQuestModal()` with detailed error notifications. Now shows "QUEST NOT FOUND - ID: xxx" if quest doesn't exist, and "ERROR OPENING QUEST: xxx" if any exception occurs. This helps debug why quests might not be opening. Cache bumped to pipboy-cache-v99.

*   **v0.111:** WASTELANDERS FIX + UNCANCEL QUEST + ACCEPT QUEST FIX. (1) **WASTELANDERS TAB FIX:** Removed reference to deleted `add-quest-btn` element in `switchSubTab()` function that was causing JavaScript error when switching to wastelanders tab. (2) **UNCANCEL QUEST:** Added `uncancelQuest()` function and UNCANCEL button in `openIssuedQuestModal()` for cancelled quests (issuer only). Changes status from 'cancelled' back to 'open'. (3) **ACCEPT QUEST FIX:** Added 500ms delay before refreshing active quests tab after accepting, allowing Firebase listener time to fire and update local `firebaseQuests` object. Cache bumped to pipboy-cache-v100.

*   **v0.112:** CRITICAL FIX - MAP BLANK + QUEST ACCEPTANCE. Root cause: Line 1108 in `switchMainTab()` referenced deleted `add-quest-btn` element without null guard, causing JavaScript error that broke entire function. This prevented map initialization and quest tab switching. Fix: Removed the reference to deleted element. This should restore map functionality and fix quest acceptance flow. Cache bumped to pipboy-cache-v101.

*   **v0.115:** BASE QUESTS REMOVED + EVIDENCE VIEWING + VERIFIED TAB. (1) **BASE QUESTS REMOVED:** Removed hardcoded "THE GATHERING" and "SCAVENGER HUNT" quests from default quests array (line 129-130). New users now start with empty quest list. (2) **EVIDENCE VIEWING:** Added "VIEW EVIDENCE PHOTO" button to `openQuestModal()` for completed quests with evidence photos. Button calls existing `viewEvidencePhoto()` function. (3) **VERIFIED TAB:** Added new VERIFIED sub-tab in quest tabs to separate verified quests from active ones. Updated `switchQuestTab()` to handle 'verified' tab. Updated `renderActiveQuests()` to exclude verified quests (only shows accepted and completed). Created `renderVerifiedQuests()` function that shows only verified quests with green border, strikethrough, verification timestamp, and evidence indicator. Cache bumped to pipboy-cache-v104.

*   **v0.116:** QUEST MANAGEMENT IMPROVEMENTS. (1) **MANUAL QUEST REMOVAL:** Added "REMOVE QUEST" button for completed legacy quests in openQuestActionModal. Added executeQuestRemove() function to delete quests from localStorage. (2) **COMPLETE QUEST ERROR FIX:** Added try-catch wrapper and null checks in completeQuest() to prevent errors when completing without photo evidence. Added check for q.issuerUid before sending verify-request mail. (3) **REJECT BEHAVIOR FIX:** Changed rejectQuest() to set status back to 'accepted' instead of 'rejected', returning quest to ACTIVE tab so user can try again. (4) **COMPLETED TAB:** Added new COMPLETED sub-tab showing completed/expired/abandoned legacy quests and completed/rejected Firebase quests (excluding verified). Shows status with color coding: green for completed, amber for expired/awaiting verification, red for rejected/abandoned. Cache bumped to pipboy-cache-v105.

*   **v0.117:** QUEST STATE MANAGEMENT FIXES. (1) **SELF-REJECT/ABANDON:** Added "ABANDON QUEST" button in openQuestModal for accepted quests. Created abandonQuest() function that sets status to 'abandoned' and removes from ACTIVE tab. (2) **VERIFIED QUESTS IN COMPLETED TAB:** Updated renderCompletedQuests() to include verified quests with green border and "✓ VERIFIED" status. Verified quests now show verification timestamp and evidence indicator. (3) **READ-ONLY COMPLETED QUESTS:** Fixed openQuestModal to only show "VIEW EVIDENCE PHOTO" button for completed/verified quests (no other actions). Prevents loop where clicking "COMPLETE QUEST" on verified quest sends it back to awaiting verification. (4) **VERIFIED TAB DEPRECATED:** renderVerifiedQuests() now shows message directing users to COMPLETED tab. Cache bumped to pipboy-cache-v106.

*   **v0.121:** QUICK CAMERA OVERLAY FOR EVIDENCE PHOTOS. Added fullscreen camera overlay that opens directly from photo picker without switching tabs. User clicks "📷 SNAP NOW" → camera overlay appears → user captures photo → photo saved to databank → overlay closes → photo picker refreshes with new photo at top → user selects it. Implementation: Created `quick-cam-modal` HTML with video/canvas elements, added `openQuickCam()`, `captureQuickCam()`, and `closeQuickCam()` functions, modified `snapNowForPicker()` to call `openQuickCam()` instead of switching tabs. Camera uses rear-facing environment mode, saves both PIP (JPEG 0.7) and RAW (PNG 1.0) versions to databank. Cache bumped to pipboy-cache-v110.

*   **v0.122:** LEGACY QUEST REMOVAL FIX + CAMERA OVERLAY DEBUG. (1) **LEGACY QUEST REMOVAL:** Updated `openQuestActionModal()` to always show "REMOVE QUEST" button for legacy quests (from localStorage), regardless of completion status. Previously only showed for completed quests. Now users can remove unwanted legacy quests like "THE GATHERING" at any time. (2) **CAMERA OVERLAY:** Quick camera overlay implementation deployed. User reported overlay not showing properly - may need additional debugging for video feed display or UI element visibility. Cache bumped to pipboy-cache-v111.

*   **v0.123:** CAMERA OVERLAY DEBUG ENHANCEMENT. Added comprehensive error handling to `openQuickCam()` function: checks if modal and video elements exist before showing, verifies camera API availability, catches video playback errors separately. Now shows specific error messages: "Camera modal not found", "Video element not found", "Camera not supported on this device", "Could not start video playback". This will help identify why camera overlay wasn't working in v0.121/v0.122. Cache bumped to pipboy-cache-v112.

*   **v0.124:** CAMERA OVERLAY UI FIX. Fixed issue where camera feed was visible but UI overlay (title, buttons) was not showing. Root cause: video element was expanding to cover UI elements due to flex layout. Fix: Added `flex-shrink: 0` and `z-index: 10` to title and button containers to prevent them from being compressed. Added `min-height: 0` to video container to allow proper flex shrinking. Added `max-height: 100%` to video element to prevent overflow. Changed video container from `flex-grow: 1` to `flex: 1` for better space distribution. Cache bumped to pipboy-cache-v113.

*   **v0.125:** CAMERA OVERLAY FILTER FIX. Fixed issue where quick camera overlay was missing the green/colored PIP-Boy filter effect. Root cause: CSS filter `var(--cam-filter)` was only applied to `#cam-video` and `#cam-canvas` (main camera tab), not to `#quick-cam-video` (quick camera overlay). Fix: Added `#quick-cam-video` to the CSS filter rule at line 428 in styles.css. Now quick camera overlay has the same themed filter as the main camera tab. Cache bumped to pipboy-cache-v114.

*   **v0.126:** OVERSEER DISPLAY OVERLAY. Added fullscreen landscape overlay for event display on laptop/tablet connected to TV. Access via OVERSEER DISPLAY button in dev controls (dev mode only). Layout: Left 75% player list with datacards, right 25% live map. Features: Filter by status (all/active/glowing/high rads/mutated/quest masters/bounty hunters), sort by quests/bounties/photos/rads/mutations/last seen/name, toggle player names on map, 30-second auto-refresh with countdown timer. Player datacards show: quests completed, bounties claimed/survived, photos taken, rads, mutations, HP, status. Map shows: color-coded player markers (green=active, amber=high rads, red=glowing, gray=offline/dead), all zones (red=hot, green=med, cyan=decon), all shared pins, auto-zoom to fit all data, stats overlay (player/zone/pin counts). Data aggregated on-the-fly from Firebase: wastelanders, quests, bounties, radzones, sharedpins. Bounties survived = count of cancelled bounties where user was target. Cache bumped to pipboy-cache-v115.

*   **v0.127:** OVERSEER DISPLAY ENHANCEMENTS. (1) **DARK MAP TILES:** Changed from OpenStreetMap light tiles to CartoDB dark tiles (same as main app map) for better visibility and theme consistency. (2) **NO AUTO-CENTER ON REFRESH:** Added `overseerMapInitialized` flag to prevent map from auto-centering on every 30-second refresh. Map only auto-centers on first load, then maintains user's zoom/pan position. (3) **PLAYER AVATARS:** Updated `renderOverseerPlayerList()` to fetch wastelanders data and display avatar images (40x40px circular with theme-colored border) next to player names in datacards. (4) **POX ECLIPSE 2026 HEADER:** Replaced "OVERSEER DISPLAY" title with large stylized "☢ POX ECLIPSE 2026 ☢" header (2.5rem font, double glow effect, letter-spacing). (5) **THEME BUTTON:** Added 🎨 THEME button in header that calls `cycleTheme()` to easily change theme in overseer display mode. Cache bumped to pipboy-cache-v116.

*   **v0.128:** QUICK CAM FILTER FIX. Fixed issue where evidence photos captured via quick camera overlay were missing the green/colored theme filter. Root cause: `captureQuickCam()` was drawing video frame to canvas without applying CSS filter. Fix: Added `ctx.filter = theme.camFx` before `ctx.drawImage()` to apply the current theme's camera filter (same as main camera). Now evidence photos have the same themed appearance as the camera preview. Cache bumped to pipboy-cache-v117.

*   **v0.132:** OVERSEER MAP REUSE MAIN MAP. Simplified overseer display by reusing the main Pox-Boy map instead of creating a separate map instance. Changes: (1) Removed custom overseer map initialization (L.map, tile layer, long-press handlers). (2) Updated openOverseerDisplay() to move #map-container element into #overseer-map-container when opening, then move it back to #tab-map when closing. (3) Removed all custom overseer map functions (overseerAddMarker, overseerRemoveMarker, overseerRemoveZone, updateOverseerMap). (4) Removed overseer map variables (overseerMap, overseerMapMarkers, overseerMapZones, overseerMapPins, overseerMapInitialized). (5) Removed custom map control buttons from HTML. Result: Overseer display now uses the exact same map with all its functionality (long-press to add markers, zone buttons, marker management, etc.) - no code duplication, all features work identically. Cache bumped to pipboy-cache-v121.

*   **v0.133:** FIX OVERSEER MAP BLANK. Fixed "updateOverseerMap is not defined" error by removing the call to updateOverseerMap() in updateOverseerDisplay(). Since we're now reusing the main map (which updates itself via its own Firebase listeners), we don't need to manually update a separate overseer map. The main map automatically shows all players, zones, and pins via its existing listeners. Cache bumped to pipboy-cache-v122.

*   **v0.134:** FIX OLD RAD ZONES NOT IRRADIATING. Fixed issue where old rad zones (created before v0.58 when 'kind' field was added) were not irradiating players. Root cause: `evalPariahField()` was checking `z.kind === 'med'` and `z.kind === 'decon'` without defaulting undefined kind to 'hot'. Old zones without a 'kind' field would fail these checks and not be processed correctly. Fix: Added `const kind = z.kind || 'hot';` to default old zones to 'hot' kind (same logic already used in `renderRadZones()`). Now old zones without 'kind' field are treated as hot zones and irradiate players correctly. Cache bumped to pipboy-cache-v123.

*   **v0.136:** FIX OVERSEER MAP INITIALIZATION. Fixed map not showing in overseer display when MAP tab never opened. Root cause: `pipMap` not initialized if user never opened MAP tab before opening overseer display. Fix: Added check to initialize map via `initPipMap()` if `pipMap` is null before moving container. Increased delay from 300ms to 500ms to ensure map is fully initialized and container is sized. Result: Map now displays correctly in overseer display even if MAP tab was never opened. Cache bumped to pipboy-cache-v125.

*   **v0.144:** PHOTO SEND TO UNLINKED + SEND TO BUTTON + EXPORT DEDUP. (1) **PHOTO SEND TO UNLINKED:** Updated openPhotoPicker() to allow sending photos to unlinked users (wastelanders with beacon data but not in rolodex). Now checks for both contact AND beacon data, only requires mutual link for contacts. Shows "(UNLINKED)" label for non-contacts. (2) **SEND TO BUTTON:** Added "SEND TO" button to photo viewer modal. Opens recipient picker with contacts and unlinked wastelanders (last 24 hours). Sends photo directly to selected recipient without going through message composer. (3) **EXPORT DEDUP:** Updated exportAllPhotos() to only export new photos (not already exported). Tracks exported count in localStorage 'pipboy-exported-count'. Only exports photos with index >= exported count. Shows "NO NEW PHOTOS TO EXPORT" if all photos already exported. Cache bumped to pipboy-cache-v133.

*   **v0.158:** BOUNTY SUBMIT EVIDENCE FIX. Fixed missing "SUBMIT EVIDENCE" button for bounty quests after scanning datacard or attaching photo. Root cause: (1) After scanning datacard, `handleBountyScan()` called `completeQuest()` which required photo evidence (the scan IS the proof for bounties). (2) After attaching photo, `openQuestModal()` reopened with same buttons (no submit button for bounty). Fix: (1) Updated `openQuestModal()` to show "📷 SUBMIT EVIDENCE" button when photo is attached (for both bounty and non-bounty quests), plus "CHANGE PHOTO" option. (2) Created `completeBountyByScan()` function that completes bounty via datacard scan WITHOUT requiring photo (scan = proof), includes optional photo if attached. (3) Updated `handleBountyScan()` to call `completeBountyByScan()` instead of `completeQuest()`. Now bounty quests can be completed by EITHER scanning target's datacard OR attaching photo evidence (or both). Cache bumped to pipboy-cache-v147.

*   **v0.159:** MULTI-STAGE QUEST VISIBILITY FIX. Fixed multi-stage quests not being visible or creatable. Root cause: (1) `openCreateQuestModal()` only offered DIRECT/GLOBAL/BOUNTY options — MULTI-STAGE was missing from the type picker. (2) `renderAvailableQuests()` typeLabel only handled global/bounty — multi-stage quests showed as "☠ BOUNTY". Fix: (1) Added "🔗 MULTI-STAGE (multiple objectives)" option to `openCreateQuestModal()`. (2) Updated typeLabel in `renderAvailableQuests()` and `openQuestModal()` to show "🔗 MULTI-STAGE" for multi-stage quests. (3) Added stages list display in `openQuestModal()` showing each stage with status icons (○ available, ✓ completed, 🔒 locked). (4) Replaced native `prompt()` calls in `editStageDetails()` with proper in-modal form (banned by project rules). (5) Added `saveQuestLevelFields()` to preserve quest title/description/timelimit when navigating into stage editing. (6) Fixed `submitMultiStageQuest()` to use `myMailUid` instead of `localStorage.getItem('pipboy-uid')` for issuerUid (consistent with v0.157 bounty fix). Cache bumped to pipboy-cache-v148.

*   **v0.160:** MULTI-STAGE QUEST VERIFICATION SYSTEM. Implemented full verification and progression system for multi-stage quests. (1) **ROUTING:** `openQuestModal()` detects multi-stage type and routes to `openMultiStageQuestModal()`. (2) **ACCEPT:** `acceptMultiStageQuest()` initializes stage progress in Firebase with first stage unlocked. (3) **LOCATION STAGES:** Photograph as evidence — opens camera, auto-attaches photo on capture. (4) **BOUNTY STAGES:** Scan target's datacard — `handleDatacardScan()` checks `window.pendingMultiStageBounty` and verifies scanned UID matches stage target. (5) **PHOTO STAGES:** Same as location — photograph as evidence. (6) **SCAN-CODE STAGES:** Scan QR code — `onScanSuccess()` checks `window.pendingMultiStageScanCode` and verifies scanned text matches stage.qrCode. (7) **PROGRESSION:** `completeMultiStageStage()` marks current stage complete, unlocks next stage, plays level-up sound. (8) **COMPLETION:** When all stages done, marks quest as completed and sends verify-request mail to issuer via `queueMail()`. (9) **BOUNTY TARGET PICKER:** Added `pickStageBountyTarget()` for selecting bounty targets from rolodex/beacon data during stage creation. (10) **VALIDATION:** Bounty stages require target, scan-code stages require QR code. (11) **UID FIX:** All multi-stage functions use `myMailUid` instead of `localStorage.getItem('pipboy-uid')`. Cache bumped to pipboy-cache-v149.

